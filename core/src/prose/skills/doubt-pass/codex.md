# Doubt pass

The flow that follows this preface is the same on every host this harness runs on. It leaves nothing to this host — no tool, no path — so there is no reference file beside it.

It runs with FRESH EYES as the `oso-doubt-pass` custom role, in a context that never made the decisions it attacks. The caller passes this wrapper's absolute path as `SKILL PATH` and the intent, surface map, and bare decisions as `ARGUMENTS`; the reviewer reads this file for itself.
