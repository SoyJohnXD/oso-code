# Triage — OpenCode

## Naming the harness's own modes and judges

Where the neutral body names the DEBUG mode as the owner of everything past attribution, it is the installed plugin skill `oso-debug` here. Point the operator at `/oso-debug`; the mode remains operator-invoked and is never entered implicitly.
