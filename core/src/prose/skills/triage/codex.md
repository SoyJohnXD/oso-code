# Triage

The flow that follows this preface is the same on every host this harness runs on. It opens by pointing you at `references/codex.md` — what it leaves to this host: how the mode it hands the operator on to is named.

This judge runs with FRESH EYES as the `oso-triage` custom role, in a context that never wrote the plan the wave executes. The caller passes this wrapper's absolute path as `SKILL PATH` and the failing check, wave slices, and WAVE START as `ARGUMENTS`; the reviewer reads this file and its reference-file binding for itself.
