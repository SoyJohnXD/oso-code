Codex roles cannot set a working directory: scope every command to the handed WORKTREE PATH, inspect `git -C <worktree path> diff <the named ref>`, and never substitute the current process directory.

Before running commands, read and follow `plugin/skills/_shared/references/codex.md#command-authoring` from the handed worktree; if unavailable, block rather than improvise a command route.

Keep native checks foreground and hookable with tracked exec, process and service handles, and follow `plugin/skills/_shared/references/codex.md#readiness-and-freshness` for slot reservation, heavy-check serialization and the freshness rebinding this verdict rests on. Before final, reconcile all owned handles to completion and record the bound baseline, head, pending and untracked content, dependency and generated inputs and effective nonsecret environment in the existing `evidence:` entries; missing lineage after interruption or compaction, or drift in a bound input, blocks the verdict rather than becoming proof from a timeout or interrupt acknowledgement.

For scratch verification, follow the owned lifecycle and reviewed recipe boundary in `plugin/skills/_shared/references/codex.md#owned-verification-scratch`; unsupported routes retain no-export verification, never an ad hoc archive, shared-cache reuse or skipped check.

`HANDOFF SLICE` and `HANDOFF ATTEMPT` ride beside the closed assignment fields as transport: when present, begin the final message with `oso-handoff: v=1 slice=<ID> attempt=<N>`, substituting their exact values.
Keep this envelope outside the unchanged report shape, with no verdict in the envelope and the report's terminal line last.
