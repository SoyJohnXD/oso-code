{
  "name": "stale gate: the stale-state signal routes a roadmap in flight to the roadmap it names, with the disarm and the clear",
  "source": [
    {
      "assertion": "roadmap_state_of auth-hardening"
    },
    {
      "assertion": "run_hook warn-stale-state.sh \"$(stale_session_input)\""
    },
    {
      "assertion": "assert_equals \"the stale-state signal routes a roadmap in flight to the roadmap it names\" \\\n  \"present\" \"$(if printf '%s' \"$hook_stdout\" | grep -F '/oso-code:roadmap auth-hardening' >/dev/null; then echo present; else echo missing; fi)\""
    },
    {
      "assertion": "assert_equals \"the roadmap route arrives with the disarm that drops the claim alone\" \\\n  \"present\" \"$(if printf '%s' \"$hook_stdout\" | grep -F -- \"--session \\\\\\\"$SESSION\\\\\\\" set roadmap=none\" >/dev/null; then echo present; else echo missing; fi)\""
    },
    {
      "assertion": "assert_equals \"the roadmap route arrives with the clear that drops the whole file\" \\\n  \"present\" \"$(if printf '%s' \"$hook_stdout\" | grep -F -- \"--session \\\\\\\"$SESSION\\\\\\\" clear\" >/dev/null; then echo present; else echo missing; fi)\""
    }
  ],
  "gate": "stale",
  "argv": [],
  "env": {},
  "state_before": {
    ".local/state/oso-code/{repo}.state": "mode=plan\nsession=other-session\nroadmap=auth-hardening\n"
  },
  "cwd": "workspace",
  "stdin": "{\"session_id\":\"test-session\",\"cwd\":\"{cwd}\"}",
  "expect": {
    "exit": 0,
    "stdout": {
      "contains": [
        "/oso-code:roadmap auth-hardening",
        "--session \\\"test-session\\\" set roadmap=none",
        "--session \\\"test-session\\\" clear"
      ]
    }
  }
}
