{
  "name": "stale gate: Codex gets the roadmap resume route in its own invocation spelling",
  "source": [
    { "assertion": "OSO_AGENT=1 run_hook warn-stale-state.sh \"$(stale_session_input)\"" },
    { "assertion": "assert_equals \"Codex gets the roadmap resume route in its own invocation spelling\" \\\n  \"present\" \"$(if printf '%s' \"$hook_stdout\" | grep -F '$oso-code:roadmap auth-hardening' >/dev/null; then echo present; else echo missing; fi)\"" }
  ],
  "gate": "stale",
  "argv": [],
  "env": { "OSO_AGENT": "1" },
  "state_before": {
    ".local/state/oso-code/{repo}.state": "mode=plan\nsession=other-session\nroadmap=auth-hardening\n"
  },
  "cwd": "workspace",
  "stdin": "{\"session_id\":\"test-session\",\"cwd\":\"{cwd}\"}",
  "expect": {
    "exit": 0,
    "stdout": {
      "contains": [
        "$oso-code:roadmap auth-hardening"
      ]
    }
  }
}
