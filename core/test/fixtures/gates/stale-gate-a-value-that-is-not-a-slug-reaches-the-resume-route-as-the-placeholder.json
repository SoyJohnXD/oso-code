{
  "name": "stale gate: a value that is not a slug reaches the resume route as the placeholder, never as itself",
  "source": [
    { "assertion": "roadmap_state_of 'Not A Slug; rm -rf /'" },
    { "assertion": "run_hook warn-stale-state.sh \"$(stale_session_input)\"" },
    { "assertion": "assert_equals \"a value that is not a slug reaches the resume route as the placeholder, never as itself\" \\\n  \"present\" \"$(if printf '%s' \"$hook_stdout\" | grep -F '/oso-code:roadmap {roadmap}' >/dev/null; then echo present; else echo missing; fi)\"" }
  ],
  "gate": "stale",
  "argv": [],
  "env": {},
  "state_before": {
    ".local/state/oso-code/{repo}.state": "mode=plan\nsession=other-session\nroadmap=Not A Slug; rm -rf /\n"
  },
  "cwd": "workspace",
  "stdin": "{\"session_id\":\"test-session\",\"cwd\":\"{cwd}\"}",
  "expect": {
    "exit": 0,
    "stdout": {
      "contains": [
        "/oso-code:roadmap {roadmap}"
      ]
    }
  }
}
