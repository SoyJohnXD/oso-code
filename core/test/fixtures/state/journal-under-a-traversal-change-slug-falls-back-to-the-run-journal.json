{
  "name": "journal under a traversal change slug falls back to the run journal",
  "source": [
    {
      "file": "tests/hooks-test.sh",
      "line": 4664,
      "assertion": "assert_equals \"a change slug carrying a path separator journals under the fallback, leaving nothing outside its own run directory\" \\\n  \"$JOURNAL_DIR>$JOURNAL_DIR/run.log>|the slug climbed out of its run directory>the slug named a subdirectory>\" \\\n  \"$journal_tree|$(journal_texts_in \"$JOURNAL_DIR/run.log\")\""
    }
  ],
  "env": {},
  "state_before": {
    ".local/state/oso-code/{repo}.state": "auto_change=../escape\nsession=test-session\n"
  },
  "cwd": "workspace",
  "argv": [
    "--session",
    "test-session",
    "journal",
    "the slug climbed out of its run directory"
  ],
  "stdin": "",
  "expect": {
    "exit": 0,
    "state_after": {
      ".local/state/oso-code/runs/{repo}/run.log": { "journal_texts_in": "the slug climbed out of its run directory\n" }
    }
  }
}
