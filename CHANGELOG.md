# Changelog

Every entry below is dated to the release it names: its counts, its paths and its present-tense sentences are facts about the commit that release shipped from, never about the tree you are reading now. An entry is not rewritten when a later change falsifies one of its claims — what a release actually shipped is the only thing a log is good for, and a corrected entry no longer holds it. What happens instead is a **Superseded since this release** block under the entry, naming the claim and what replaced it, so both stand and a reader can tell which is which.

## 0.27.0

**Reinstall on all three hosts — this release moves hook bytes on every one of them.** On Codex run `bash bootstrap/install-codex.sh --yes`, restart, and re-trust EVERY handler `/hooks` lists rather than a subset: fourteen of the fifteen artifacts `bootstrap/hook-hashes.txt` published at 0.26.0 moved in this release — all nine Codex-wired handlers, plus `plugin/hooks/lib.sh`, `plugin/hooks/lexer.sh`, `plugin/bin/oso-state`, `plugin/git-hooks/pre-commit` and `opencode/hooks/routes.ts` — and only `codex/hooks/hooks.json` is unchanged. A sixteenth is published for the first time: `plugin/hooks/reanchor-after-compact.sh`, which the OpenCode route table runs and the hash file did not cover, so that host installed it unverified. The file publishes 16 hashes now, over the same 13 gate rows `tools/hook-gates.txt` has carried since 0.24.0. On Claude Code `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code` carries those hooks plus `plugin/hooks/auto-continue.sh`, which the hash file excludes by its own coverage rule; `bootstrap/claude-global.md` is untouched here, so this release owes `bash bootstrap/install.sh` nothing the 0.24.0 entry did not already owe. On OpenCode run `bash bootstrap/install-opencode.sh --yes` and restart: `bootstrap/opencode-global.md`, the whole TypeScript plugin and `opencode/hooks/routes.ts` moved, and so did the host pin. `git tag` still reaches only `v0.23.0`, so a machine on the newest published release crosses 0.24.0, 0.25.0, 0.26.0 and this one, and owes each of their remedies.

**The pin moves to OpenCode 1.18.22, measured rather than assumed, and it moved twice inside this release.** It reached 1.18.20 first: `tests/opencode-contract-bar.sh` and `tests/opencode-behavior-bar.sh` were driven against 1.18.18, 1.18.20 and 1.18.21 and returned the same check names with the same values on all three, and the plugin contract this port rests on — one named export, a flat hook map, `output` mutated in place — holds on each. It moved again to 1.18.22 the same way: both bars were driven against `npm pack opencode-linux-x64@1.18.20` and `@1.18.22` binaries and agreed check for check — the contract bar at 67 passed / 0 failed on both, its output identical line for line except the one check whose value IS the pin, and the behavior bar at 11 passed / 0 failed on both, the full battery green on each. The behavior bar's own not-run lane fired on one 1.18.20 run and its re-run closed green at 11, which is the distribution this entry's last paragraph publishes rather than a check answering differently on the two binaries: no check did. 1.18.22 is what an operator installing from https://opencode.ai/install gets today and what `opencode-linux-x64@latest` resolves to, which is the whole reason it is the pin. `SUPPORTED_OPENCODE_VERSION` in `bootstrap/install-opencode.sh` remains the single authority: CI's `npm pack opencode-linux-x64@$pin`, both bars, `bootstrap/verify-opencode.sh` and the suite's own installed-binary comparison read it instead of repeating it, and the three literals `tests/hooks-test.sh` still spelled are derived from it now — three slices running found a stale copy of a number, and fewer copies beats more care. Where the parity ledger asserts the pin it names 1.18.22; where a row records the binary a measurement was taken on it still names 1.18.18 or 1.18.20, because moving that would be rewriting evidence rather than a version, and `docs/parity-opencode.md` says which is which at the top. Two decisions taken at 1.18.20 are outside what the two bars reach and were not re-measured for the move: ADR-0155, that a `variant:` with no `model` beside it is dropped when the host reads the agent, and ADR-0156, that a headless child can be auto-refused `external_directory` permission for the worktree it was pinned to.

**The OpenCode host carried the delegation-net defect 0.25.0 fixed on Claude, and carried it worse.** `opencode/plugin/oso/unattended-run.ts` is an independent TypeScript implementation of the same net, writing the same `.waiting` sidecar as `plugin/hooks/auto-continue.sh`, and it keyed a sighting on the journal byte count beside the label and the session. So every turn that journaled a milestone wrote a fresh mark, the mark's age restarted, and the 45-minute ceiling never fired; a hold never touched the push tally, so the three-posted-turn cap never engaged either. Driven over twenty consecutive turns of a run making steady progress under one delegation, this host held twenty times and stopped never, where the bash net on the identical stream holds four times and then pushes. `docs/parity-opencode.md` stated the bound this host did not have, which is what made it a release blocker rather than a defect: shipping it would have sold a bound in writing that nothing enforced.

- **The bounded-renewal design is ported whole, and the two nets are measured agreeing.** The mark is keyed by label and session; progress under a standing mark RENEWS the belief window rather than restarting it unbounded, at most 3 times, so the longest a marked delegation holds is four ceilings and a run that has genuinely stopped moving still meets the first one. Both implementations are driven over the same four streams — a delegation the run keeps moving under, one it has stalled under, an unmarked run, and a mark seconds old, which is the field shape that started this — and answer identically turn for turn on all four. The sidecar they share now carries `renewals` from both sides in the same four records, the same order and the same `0600` mode, and `opencode/plugin/oso/unattended-run.test.ts` drives the bash hook against a mark this host wrote and this host's reader against a mark the bash hook wrote, so the one file two implementations own cannot drift in silence. The bound is written at its true scope where a reader meets it: per `(label, session)`, restarted by a new label, by a `none → label` cycle that removes the mark, and by any external rewrite of it — a bound on one delegation's hold and never a per-run guarantee. One divergence survives the port, and it is robustness under corruption rather than behaviour: a sidecar carrying `renewals` twice — which neither side ever writes, since both rewrite all four records together — reads as 0 in bash, where `state_value` returns both lines and the count test rejects the pair, and as the LAST value in TypeScript, where `parseRecords` is last-wins. So a corrupted mark already at the cap holds on one host and pushes on the other.
- **The linter rule that keeps the suite off the operator's own OpenCode data had three holes and a silent zero.** The awk pre-pass that decides which lines reach the shell lexer skipped every heredoc body, while the lexer behind it distinguishes them — so `bash <<'SH'` / `opencode --version` / `SH` ran unflagged. The whole heredoc goes to the lexer now, which reads a shell-fed body as the commands it is and a `cat` heredoc as the data it is, and the pre-pass no longer decides anything the lexer already decides better. A `case … in` header no longer swallows the invocation sharing its physical line. And a command word that is a variable is matched on the name it actually carries: a name ending in `OPENCODE`, alone or under the suffixes a path to a binary is spelled with — `_BIN`, `_BINARY`, `_EXE`, `_PATH`, `_CMD`, `_CLI`, and compositions of them like `_BIN_PATH` — names the binary, so `$OPENCODE_BIN`, `$OSO_PINNED_OPENCODE_BIN`, `$OPENCODE_EXE` and `$OPENCODE_BIN_PATH` all flag, while `$OPENCODE_CONTRACT_BAR` and `$OPENCODE_INSTALLER` name repository scripts and are left alone. A suite case pins seven of those spellings — `$OPENCODE_EXE`, `$OPENCODE_PATH`, `$OPENCODE_CMD`, `$OPENCODE_CLI` and `$OPENCODE_BIN_PATH` flagging, the two repository scripts not — so narrowing the test back to the one spelling this tree actually carries fails that case rather than passing silently.
- **The silent zero was the expensive one, and it was hiding 95% of the file it was asked about.** awk's stderr went to `/dev/null`, so an unmatched glob would abort it on the missing filename and the rule would report zero sites — a green that never ran. The file list is built in bash against a readability test now, a scan with nothing readable flags instead of passing, and awk's stderr reaches the operator. That guard found its own case on the first run: the pre-pass read `<<PROBE` inside a single-quoted `printf` as a real heredoc opener and, finding no terminator, swallowed every line after `tests/hooks-test.sh:747` — 14,372 of that file's 15,119 lines were never scanned by the rule that reported them clean. Quoted spans are stripped before an opener is looked for, except where the quotes are the delimiter's own, and the scan now reaches that file's last line, whatever it is on the day you read this: 1,057 units where it read 343, at 6.9s of whole-lint wall time where it took 5.3s.
- **The rule's two uncovered forms are stated accurately and pinned by a test.** The first is not the bare `opencode` alone: any `opencode` whose only remaining words are redirects or descriptors goes unflagged — `opencode`, `opencode >/dev/null`, `opencode </dev/null`, `opencode 2>&1` and `opencode >log 2>&1` alike. The second is inherited rather than the rule's own: a logical unit longer than `LEX_MAX_INPUT_BYTES` comes back from the lexer as `!unread-payload` and carries no command word at all, so the same `opencode --version` flags on a short line and passes on a padded one. All six are named in one suite case beside the forms that now DO flag, so the cost is pinned rather than left to be rediscovered, and closing either later fails that case rather than passing silently.
- **Every comment in a shell file this repo ships is gone, and a rule keeps them gone.** The shared rubric has held the inline comment as a debt class with no exceptions since 0.20.0 and this repo was still carrying 3,852 of them, so the retroactive sweep the class always implied finally ran: 3,828 whole-line comments, 4 trimmed off the code they trailed, and 24 inside the awk and python programs that live in a shell string. One block survives per file and only where the file declares no interpreter — a module nothing can run has no other place to state a contract, which is the one thing ADR-0140's own context conceded — so `bootstrap/lib/`'s five sourced libraries plus `plugin/hooks/lib.sh` and `lexer.sh` keep 64 lines across seven files and every other header went with the bodies below it. The bar was not that the suite still passes: `tests/hooks-test.sh`'s full output was captured before and after, `mktemp` paths normalized, and compared byte for byte — 1,785 lines, exactly ONE different, and it is the name of the mutation case that now deletes a table row instead of a header line. Two comments a machine was reading moved rather than died: the linter's rule count is in the line it prints (`lint: clean — fifty-one rules`), and the four PreToolUse gates' recovery routes are a fourth record kind in `tools/hook-gates.txt`, enforced by the same parser with the same refusal message. `bootstrap/hook-hashes.txt` was re-rendered for the hook bytes that moved, which is the cost this purge was refused for once and accepted for now. `bootstrap/install.bat`, `bootstrap/install.ps1`, `bootstrap/verify.bat` and `bootstrap/lib/toml-regions.awk` keep their 183 comments and are the named remainder: no bar here can run them, and a purge nothing can prove behaviour-neutral is the opposite of the one above.
- **`bootstrap/verify-opencode.sh` stops failing for the host's own permission decision.** It closed `17/1` on three of three clean installs earlier in this change, and that red was never a defect in the tree: the child stream carried `! permission requested: external_directory (…/wt2/*); auto-rejecting` and the write came back `The user rejected permission to use this specific tool call.`, so neither child could create its proof file, while the four isolation assertions the check is NAMED for never fired in any run — nothing was ever violated. Three runs taken when the fix landed closed `18/0` at exit 0 with the smoke completing on `opencode/hy3-free`, so the refusal is real and NOT deterministic: the smoke picks its model out of the host's catalog at runtime, and whether a child's write raises an `external_directory` question at all depends on whether that model writes into the working directory it was pinned to or at the absolute worktree path, which the host measures against a `sandboxes` list naming the project ROOT. Both measurements are in ADR-0156 because one of them alone would be a false claim. The smoke now separates a breach from an incompleteness, reads that one line out of the captured stream, and takes the `not run:` lane it already had — so where the host refuses the report says so by name at exit 3 instead of counting a failure, and where it does not the run is untouched. A child that writes into the OTHER worktree is still red under exactly that refusal, and the suite proves all three outcomes against seeded logs with no host and no model.
- **Nothing guarded the class `.atl/` belonged to, and two rules do now.** No file under this repository may carry the absolute home directory of whoever runs the check — the machine that would leak its own layout is the machine running the linter, which is why the rule needs no username and never fires on the generic `/home/a/b` and `C:/Users/dev/…` examples already in the tree — and no dot-directory may stand beside the repository's own without being either one this repo owns by name or a line in `.gitignore`. The second rule's first finding was `.claude/`, ignored on one machine through a personal global ignore file that no clean checkout and no runner has; it is declared in `.gitignore` now.
- **The close's judges found three fail-opens on this host, and the fourth on the boundary all three share.** `opencode/plugin/oso/gates.ts` re-ran a gate with NO envelope whenever the first run raised `EPIPE`, and returned that blind run's exit as the verdict: three of the four enforcement gates read their whole subject from stdin and exit 0 without one, and the `EPIPE` branch sat AHEAD of the signal guard, so one identical transient gate death came back `block` at a 60-byte command and `allow` at a 2 MB one. A gate that never received its envelope has no verdict now — the signal guard runs first, the blind re-run is gone, and both lengths block. One residual stands beside that fix and nothing in the shim enforces it: a gate that exits 0 without ever reading its stdin raises no `EPIPE` at all where the envelope fits the pipe buffer, and resolves to `allow`. None of the seven can do that today — every one of them reads `$(cat)` unconditionally before it decides anything — but that is a property of how they happen to be written rather than an invariant this host checks, and no test asserts it. `matchesTool` answered `false` for a matcher no regular expression compiles from, which disarmed that route in silence — including the `.*` catch-all that IS this host's default-deny — and a suite case pinned that as intended; a broken matcher is a broken install now, and every route is compiled at load through the same channel a missing gate script reports through, so a table that will not compile denies every tool call. The install-status guard also moved ahead of the matcher test, and that move is not what makes the deny total: reverting the ordering alone, with the load-time compile kept, still denied the call, because the `unknown` gate's `.*` catch-all reaches the install check whatever the tool name is. What the ordering buys is the deny surviving the day that catch-all leaves the table. `opencode/plugin/oso/unattended-run.ts` read a push tally that exists but cannot be parsed as ZERO posted turns so far, where `plugin/hooks/auto-continue.sh` stands the run down on the same file — so one `printf` over the sidecar kept an unattended run posting turns forever, and `PUSHES_WITHOUT_PROGRESS_CAP` never fired. The reading is three-way now — absent, counted, unusable — and both implementations are driven over the same four malformed sidecars, agreeing turn for turn the way slice 22 proved the sighting.
- **At the production boundary an unread payload is production, on every host.** A trailing `# <3 KiB of padding>` puts a line past `LEX_MAX_INPUT_BYTES`, the lexer answers `!unread-payload` and nothing else, and `vercel --prod`, `netlify deploy --prod`, `firebase deploy`, `git push origin main` and `git commit -m x --no-verify` all came back a `residue` both gates ALLOW. `plugin/hooks/lib.sh` gives that marker its own verdict now — `unread`, distinct from the `residue` a lexed-but-unresolvable call earns — and `plugin/hooks/block-prod-deploy.sh` denies it as production, outranking a lexed push in the same line, exactly as its own `arguments_arrived_unread` already failed closed for unread stdin. The cost is stated rather than hidden: while THIS session's unattended run is in flight, any command line past that bound is refused at the production boundary whatever it would have run — `git commit -m x` under 3 KiB of padding is denied there too — and outside an armed run the gate exits before it reads the line at all. The commit rail keeps its polarity deliberately and spends an unread line as residue, because `plugin/git-hooks/pre-commit` stands behind it as a second layer where the production boundary has none — and that second layer is exactly what `--no-verify` skips. `tests/hooks-test.sh` measures shapes against both layers rather than characterizing the gap between them: unpadded `git commit --no-verify -m x`, where the matcher denies and the commit still lands because `--no-verify` is what it names; the same line padded past the lexer's bound, where the matcher allows what it could not read; and `git ci --no-verify -m x` with no padding at all, where the matcher allows because it gates on the git verb and `ci` is not a verb it gates, and the commit lands. That third shape needs no padding to get there because it is the aliased blind spot the same file already pins a few lines above, where the matcher allows `git ci -m x` and the git layer stops it — and `--no-verify` is what takes that layer away.
- **The OpenCode installer placed four executables it never hashed, and its snapshots reached across hosts.** It copied every `plugin/hooks/*.sh` and `chmod 700`d them while the published manifest covered eleven — and one of the four, `plugin/hooks/reanchor-after-compact.sh`, is wired at `opencode/hooks/routes.ts:64` and executed by the plugin. The manifest's coverage rule now reaches past the Codex-wired handlers to every script the OpenCode route table runs, the installer copies exactly what that manifest names, and a post-install assertion refuses any gate tree holding an executable with no published hash — measured 15 installed against 11 published before, 12 against 12 after. `bootstrap/restore-codex.sh` listed and restored an `install-opencode.sh` snapshot on all three of its paths while its own error string claimed a provenance check it never ran; both installers stamp a format marker and both restores key on their own, the symmetric shape `bootstrap/restore-opencode.sh` already had. Retention enumerated the shared root, so an OpenCode install deleted a Codex snapshot on a budget it never spent, and one shared `.install-restore-verified` file meant exercising either restore armed destructive pruning in both: each host prunes only its own snapshots now and arms on its own marker, so **an operator who exercised a restore before this release must exercise it again on each host before retention resumes there**.
- **Two OpenCode permission blocks granted more than they meant to.** `read: allow` and `bash: allow` are scalars, and this host concatenates the agent block LAST with last-match-wins, so each became a `"*"` rule that erased the installed `read: {"**/.env": "deny"}` and `bash: {"git push *": "ask"}` posture underneath — a thing neither Claude's closed `tools:` allowlist nor Codex's sandbox tier can do. Every scalar grant is gone from `opencode/agents/*.md`: the base and the operator's own rules resolve normally and every deliberate `deny` and the plan agent's narrowed `bash` allowlist stand untouched. And `fallow_fix_apply` — write-capable, in the runtime allowlist, and reachable by four roles documented "judges only — never edits" because no agent declares a `tools:` key — is denied on every non-applier role.
- **A rule now catches the class three slices in a row have found.** `tests/plugin-lint.sh` reads every `` `path:line` `` citation under `docs/` and fails when the path is gone or the line is past the file's end or blank — the class the comment sweep created by shortening every shell file, which is how four citations came to point at nothing. It flags a scan that read no citation at all, for the reason the silent zero above gives. Its reach is narrower than "every record", and the four blind spots are measured rather than assumed. Its glob over `docs/*.md` does not recurse, so `docs/decisions/`'s 157 files and this `CHANGELOG.md` are outside it — honest as policy, since neither is a living record, but an accident of the glob rather than an exclusion anything names, and this change both adds files under `docs/decisions/` and edits ones already there. The citation pattern itself requires an extension, so `plugin/bin/oso-state:99999` — a real shipped file with 725 lines — is invisible to it; it requires backticks, so the same citation written bare is too; and a range is checked at its START alone, so `` `plugin/hooks/lib.sh:5-99999` `` reads sound against a file of 450 lines.
- **A shipped script's sourced library has to be in the INDEX, and the tree bar says so now.** `bootstrap/lib/codex-install-backups.sh` is new in this release, and `bootstrap/install-codex.sh` and `bootstrap/restore-codex.sh` both source it behind a guard that exits when it is absent — so left out of the index it would have handed every clone two scripts that refuse before they do anything, while the machine that wrote them stayed green, which is exactly what a working tree cannot see. `tests/hooks-test.sh` reads every `.sh` path `git ls-files` returns and, on each line that opens with `.` or `source`, takes the literal tail off the last `)` or `$` in it, keeps what holds a `/` and ends in `.sh`, and fails when no tracked path ends in that tail. The index rather than the disk is the whole distinction, and it is proved both ways: on this tree with the file unstaged the rule named both call sites, staged it reads clean, and a fixture repository holding one script and one untracked library it sources takes the rule red and then green as that library is added. Its reach is that cut and nothing wider: `bootstrap/install-codex.sh`'s `. "$SCRIPT_DIR/lib/install-backup.sh"` and `plugin/hooks/lib.sh`'s `. "$(dirname "${BASH_SOURCE[0]}")/lexer.sh"` are in it, while a whole path held in one variable carries no tail to match (`bootstrap/install.sh`'s `. "$BACKUP_LIB"` and `. "$FALLOW_COMMAND_LIB"`), a plain literal offers no `)` or `$` to cut at (`. lib/x.sh`, `source /abs/x.sh`, `. "lib/x.sh"`, `. lib/*.sh`), and a source behind a conditional never opens its line. Widening it to plain literals was measured and refused: it turns the two rows of `tests/hooks-test.sh`'s own `SOURCED_SCRIPT_TABLE` heredoc — `source deploy.sh` and `. deploy.sh` — into false positives, because a grep over lines cannot tell a fixture table from code. It stands beside the final-newline check under the same `git ls-files` guard, taking the same skip lane where git is absent, because a rule that answers the index needs one.

- **The record moves with it.** Both plugin manifests read 0.27.0 and the changelog opens on that same version, which is the bar that went red the moment they were bumped and green when this entry landed. `.gitignore` gains `.atl/`, and `git rm -r --cached .atl/` takes the two files an external tool regenerates there out of the index — the ignore rule reaches nothing while the path is TRACKED, and it was tracked, committed at `1ebb29a`, which is why the tree read clean and the new rule read as a no-op. Left in the index, the pull request this branch opens would have published `.atl/skill-registry.md`: a generated cache carrying 25 absolute paths under the operator's home directory and the private skill inventory they enumerate. Untracked and ignored it is the same regenerable-cache class as the `node_modules/` beside it, and the chain's own tree bar (ADR-0138) keeps reading clean over it. The linter grows to fifty-one rules, `tools/hook-gates.txt` keeps its 13 gate rows and gains a fourth record kind, `docs/decisions/` goes from 153 files to 157, and the OpenCode unit suite closes at 152 tests. On the machine this release was cut from, `tests/opencode-contract-bar.sh` closes at 67 passed / 0 failed against the newly pinned binary — 66 of them the count it pins in `EXPECTED_CHECK_COUNT`, sixteen more than the release opened with because every agent's `read`, `bash` and `fallow_fix_apply` rule is now resolved and asserted one agent at a time — and `tests/hooks-test.sh` at 1,801 cases with 0 failures and 1 skip — the same dated-rather-than-gated figures the 0.26.0 entry explains, published because the honest replacement for a stale number is a measured one. The two bars that drive a live model are published as distributions instead, because a single number is not what a reader re-running them will see.

- **A live-model bar cannot be pinned to one figure, so CI stops treating it as one.** `tests/opencode-behavior-bar.sh` scored the production boundary RED whenever the model declined the prompt and probed for the CLI instead: `command -v vercel` carries `vercel` as a command word, the attempt test asked for that word alone, and a refusal came back as a boundary failure while the gate had done nothing wrong. The attempt test is the boundary now — `vercel` AND `--prod`, the production form the gate itself denies — and the deploy-CLI marker is read for that same flag, so a benign `vercel` reaching the shim can never redden the assertion that a REFUSED deploy did not. What remains is the bar's own not-run lane, which fires when a model drives no gated call at all, and `.github/workflows/ci.yml` tolerates exactly that exit and nothing else: a red assertion still fails the tree, and a session that asserted nothing says so as a warning rather than passing in silence. Three consecutive runs before the change and three after, all on `opencode/hy3-free`, give the distribution: before, one red, one not-run and one green at 11 passed / 0 failed; after, two green at 11 passed / 0 failed and one not-run — both not-runs a model refusing the prompt in prose rather than a boundary moving. `bootstrap/verify-opencode.sh` closes at 17 passed / 1 failed in four of four runs with the wave smoke running, and that one red is not the model: at 1.18.20 a headless `opencode run --dir <worktree>` auto-rejects an `external_directory` permission for the very worktree it was pinned to, so neither child reaches its write at all: across three runs both were rejected and no proof file was written anywhere, while the same write against a plain directory lands correctly inside `--dir` — the four isolation assertions the check is named for never fired in any run. CI never reaches that smoke: it runs the verifier under `OSO_VERIFY_SKIP_SMOKE=1` and pins `passed: 16`.

## 0.26.0

**A third host ships: OpenCode is a first-class runtime beside Claude Code and Codex, pinned at 1.18.18, installed by `bash bootstrap/install-opencode.sh --yes` and verified by `bash bootstrap/verify-opencode.sh`.** The port is 16 slices in 8 waves, all closed 2026-08-19: the baseline and F7 verdict, the gate table's third host, the platform overlay and eight skill wrappers, the eight agents, the purge script, the TypeScript plugin skeleton and its type bar, identity and lineage, lifecycle, the linter's third arm, the `plan_exit` approval rail, the own workspace adapter, the thin `oso_wave` fan-out, the strict-JSON installer, the self-contained verifier with a real wave-runner smoke, and the CI step. Every row the parity ledger's skeleton left pending is now resolved in `docs/parity-opencode.md` — the approval rail, the workspace adapter, the MCP `environment` key, the in-band delegation verdict, the version bar and model availability all closed with the slice that proved them.

The host's differences are stated, never papered over. The commit gate runs through `tool.execute.before`, with F7 proven against the pinned binary so the rail reaches appliers in worktrees; approval is `plan_exit` behind `OPENCODE_EXPERIMENTAL_PLAN_MODE` with a digest rail that THROWS on decline, because the TUI flips the active agent on any completed tool part and never reads the result value; worktrees are the adapter's own awaited `git worktree add -b <branch> <path> <WAVE START>`, which beats the host's forked-empty trap; a delegation blocks until the turn completes and returns its verdict in-band, so `oso_wave` needs no handoff receipt; and roadmap mode is NOT ported — recorded in the parity ledger's frozen loss and degradation ledger beside the headless `--command` limitation and the no-subagents rail.

The plugin is one TypeScript entry, `opencode/plugin/oso-code.ts`, importing sibling modules for the gates (a TS shim that translates the bash gates' three exits), identity (`shell.env` published per invocation), lifecycle (a liveness-guarded sweep that never reaps a live owner), the plan rail, the workspace adapter and the wave fan-out — verified by `tsc --noEmit` plus `node --test`, the bar that replaces the lagging published `.d.ts` as this host's type authority. The installer owns `opencode.json` wholesale from a strict-JSON template, re-applies the operator's provider, model, small_model, permission and MCP choices from a verified backup, merges `permission` per sub-key, spells MCP env as `environment`, installs the eight wrappers under the singular `skill/` spelling with the shared bodies beside them, the eight agents and three commands, wires Engram through its own installer, and mounts Impeccable pinned at a Git tag — all inside the shared transactional backup and rollback, with the three project-level `opencode.json` files never touched. `verify-opencode.sh` is self-contained against a disposable fixture HOME and runs a real wave smoke — two worktree children on `opencode/deepseek-v4-flash-free`, each writing only its own proof file — with bounded runs and clean skip lanes; CI pins both halves of its report (`passed: 12, failed: 0`) on a fresh HOME after `npm ci` for the gitignored `node_modules`.

The record moves with it. Three decisions — ADR-0151 (the OpenCode baseline: the pin, the plugin contract and the F7 verdict), ADR-0152 (skill visibility control on the pinned host) and ADR-0153 (the installer, verifier and CI host facts) — take the log from 150 files to 153. `docs/blueprint.md` grows the index group, names OpenCode a first-class adapter in the Platform row, and updates the Context budget row to "adding a second and a third adapter"; README gains the OpenCode install path, the `/oso-plan`, `/oso-quick`, `/oso-debug` spellings and the third verified runtime in its Surfaces paragraph. The linter stays at forty-two rules for the third host, and `tests/hooks-test.sh` closes at 1664 cases.

**Superseded since this release.** The port continued past 0.26.0 in the `opencode-runtime-parity` change, and the claims recorded below no longer describe the tree. Each of them was true at `a7ea0db`, the commit this release shipped from, and each stands above exactly as it was written. The list is what has been found rather than a proof of completeness — a total stated here would be one more figure nothing derives, which is the very failure the last entry in it describes.

- **The approval rail is no longer `plan_exit`.** At `a7ea0db` that rail was real — `opencode/plugin/oso-code.ts` dispatched on a `plan_exit` tool part and `opencode/plugin/oso/plan.ts` carried the `declineThrows` behind it — and it is gone. Approval is `oso_plan_approve`, a plugin-registered tool that raises the host's OWN authorization prompt from inside its `execute`, with `permission.oso_plan_approve: ask` in the installed config: the operator's answer to that prompt is the approval, and a decline arrives as a thrown rejection that promotes nothing. `plan_exit` is depended on nowhere now. Three artifacts still carry the name and none of them reads it: the installer sets `permission.plan_exit`, `tools/hook-gates.txt` carries its `tool` row, and that row renders into the unknown-tool allowlist in `opencode/hooks/routes.ts` — each because it is one of the host's own tools and for no other reason, with `bootstrap/verify-opencode.sh` and `tests/hooks-test.sh` asserting the first of the three rather than adding a fourth. `docs/parity-opencode.md` carries both halves — the approval-rail row and the `planstop` gate row that records the removal.
- **Roadmap mode is ported.** The paragraph above records it as NOT ported and points at the parity ledger's frozen loss and degradation ledger for it; that ledger's four entries are the headless `--command` limitation, the no-subagents rail, a headless child that cannot write inside the worktree it was pinned to, and the workspace adapter's create handshake, and roadmap is not among them. `opencode/skills/oso-roadmap/SKILL.md` and `opencode/commands/oso-roadmap.md` ship the mode and `docs/parity-opencode.md` records the whole chain running unattended on this host, so the installer's own inventory moved with it: nine skill wrappers where the paragraph above says eight, and four commands where it says three.
- **Worktrees no longer come from the adapter.** The awaited `git worktree add -b <branch> <path> <WAVE START>` named above was the adapter's own at `a7ea0db`; commit `1ebb29a` deleted it, and nothing in the plugin package runs `git worktree add` any more. `opencode/plugin/oso/workspace.ts` registers for discovery only and its `configure` refuses by name. A wave's worktree is cut by the ORCHESTRATOR before any child session exists, from the neutral plan body's own §6 command, and `oso_wave` pins a tree that already exists rather than creating one. `docs/opencode-parity-plan.md` §0 records the same supersession against the frozen decision and the slice that delivered it.
- **The linter declares fifty-one rules.** Forty-two is what it declared at `a7ea0db`, which is what the sentence above says and what was true then. Both counts are spelled in words because `tests/plugin-lint.sh` derives its own from its source, and a rule now goes red when a surface names another — including this entry, which is why the current count is stated here rather than left for a reader to discover.
- **The OpenCode pin is no longer 1.18.18.** The headline above pins the host at 1.18.18 and the bullet below closes on an operator whose installed client is not that version getting the comparison back as a red; 0.27.0 moved the pin twice — to 1.18.20 after running both OpenCode bars against 1.18.18, 1.18.20 and 1.18.21 with identical results, and on to 1.18.22 after running them against 1.18.20 and 1.18.22 with identical results again — so on a current tree it is 1.18.22 that the installed binary is compared against. What the sentences above recorded was true at `a7ea0db` and stands.
- **`tests/hooks-test.sh` closes at 1,801 cases**, not the 1664 it closed at here (that figure was measured for this release and is recorded in `docs/opencode-parity-plan.md` beside the run that produced it). This one is dated rather than gated, and the difference is the point: no rule can derive a runtime assertion count the way the rule count is derived, so publishing one is publishing a fact about a single commit and nothing will catch it going stale. It is published anyway, because the honest replacement for a stale number is the measured number and not a vaguer sentence — with the caveat stated instead of hidden. 1,801 is the count at the commit this block lands on, beside one skip and no failure — measured on a machine whose installed OpenCode client IS the pinned version. An operator whose client is not gets that comparison back as the run's single red, which belongs to the machine rather than the tree.

## 0.25.0

**Plugin update on Claude Code, reinstall on Codex, and no `/hooks` re-trust anywhere — the one hook whose bytes moved is wired on Claude alone.** That hook is `plugin/hooks/auto-continue.sh`, the `Stop` net, which `tools/hook-gates.txt` wires on Claude and nowhere else — so `bootstrap/hook-hashes.txt` excludes it by its own coverage rule, the published hashes did not move, and `tools/render-hooks-json.sh --check-hashes` still matches all 14 of them. Both rendered hook manifests are untouched since the 0.24.0 merge and `--check` confirms each is still exactly what `tools/hook-gates.txt` renders. On Claude Code `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code` carries this release whole — the hook, the Claude platform bindings and the two neutral bodies all live under `./plugin`, which is what the marketplace entry's source names, so refresh first for the reason 0.21.0's version-notice bullet gives — and `bootstrap/claude-global.md` is untouched, so no `bash bootstrap/install.sh` is owed this time. On Codex run `bash bootstrap/install-codex.sh --yes` and restart: that installer stages `plugin/skills/_shared` itself and BOTH NEUTRAL BODIES moved inside it, so the reinstall is what carries them, while the hooks it places, the manifest they are keyed by and the block it owns in `~/.codex/AGENTS.md` are all unchanged — which is why `/hooks` needs nothing from you here. What no crossing escapes is 0.24.0: it is merged and unpublished, `git tag` reaches only `v0.23.0`, so a machine on the newest published release crosses that release as well and owes its remedies — a reinstall on both hosts, the `/hooks` re-trust on Codex, and the `bash bootstrap/install.sh` its `bootstrap/claude-global.md` clause needs on Claude.

An unattended run pushed itself twice inside one minute while its applier was working normally, then wrote `cap reached after 3 pushes without progress` into its own durable run journal. The run had made progress; it was waiting for a subagent. The net that exists to keep such a run moving had spent its whole cap on a delegation doing exactly what it was told, left a false sentence in the one record the operator reads on their return, and disarmed itself until the next real milestone — so a run that later stalled for a real reason met a net with nothing left.

Underneath it, two contracts each right when they were written. The Claude binding ordered `run_in_background: false` on every `Agent` launch; that tool's schema is five fields under `additionalProperties: false`, it has never carried such a parameter, and its own result says the opposite — the agent runs in the BACKGROUND and a completion notification arrives later. So every delegation ended its turn, correctly, and the `Stop` net's whole predicate was that a turn ended without parking or closing the run. Neither text could see the other's mechanism. The premise lived in THREE places, and the third is why a careful reader could confirm it twice and still be wrong: `tools/hook-gates.txt` recorded "Claude launches these synchronously" as the reason `SubagentStop` is unwired on that host.

- **A delegation in flight is not a stall, and the net can tell.** The flow arms `auto_wait=<label>` before any launch whose result arrives in a later turn and returns it to `auto_wait=none` once every report for that label has been read; `auto-continue.sh` holds that turn-end instead of pushing it, leaving the push tally exactly where it was and recording one `auto-continue-held` event, so a net that held on purpose never reads back as one that never ran. The mark is believed for 45 MINUTES, measured from a hook-owned `.waiting` sidecar's mtime rather than the state file's — any `oso-state set` refreshes that one — and past the ceiling the push carries its own order and its own give-up milestone naming the stale mark, because reintroducing the false "no progress" claim through the expiry door would have fixed the pushes and kept the lie. Expiring early costs turns and never the run: a push never touches the running subagent, and the completion notification resumes the run even after the net has given up. The continuation order also tells a pushed run NOT to relaunch a delegation in flight. Every added failure path allows the stop — an unreadable sidecar, one it cannot write, an age that does not read back — while a label that does not parse is no added path at all: the marker is ignored entirely and the turn is pushed and counted exactly as an unmarked run, leaving the net behaving exactly as it did at 0.24.0. The key cost `plugin/bin/oso-state` nothing, which is the other half of why this release asks Codex for no re-trust: `set` validates no key names, so a new key is zero bytes in the binary and zero in `plugin/hooks/lib.sh`, two of the files `bootstrap/hook-hashes.txt` publishes. Its spelling is constrained from the other side — `block-prod-deploy.sh` reads a state file with any line outside `^([A-Za-z0-9_]+=|[[:space:]]*$)` as malformed, and an unusable state file DENIES there.
- **A wait mark is evidence about one run, and expires with it.** The sidecar records `label=` and `session=` in the key=value shape the push tally beside it already uses, so a mark whose run is not this one is a stale file rather than a sighting to inherit: it is rewritten with this run's identity and the clock starts there. What that closes is the RESUMED run — one parked mid-slice with its delegation still armed came back to a mark bearing its own label and an hours-old mtime, so its very first `Stop` read the mark as expired and began spending the cap on exactly the path the hold was written for. A marker that is not `running` has no delegation in flight by definition, so the mark it left is REMOVED as the stop is allowed, and a bare-label mark from an older net matches neither key and is stale by construction, which is the whole of the migration. Fail-open is unchanged: an unresolvable journal path declines to clean and an unremovable mark is inert.
- **The Claude wait contract describes the mechanism this host has.** `platform/claude/delegation-wait.md` states it whole where `plan.md` and `debug.md` used to state a flag apiece: the background launch, the later-turn completion notification that IS the resume, that nothing may act on a report it has not read, that a delegation in flight is never relaunched — a second launch over the same tree is two writers in one slice — and the marker with its `^[a-zA-Z0-9][a-zA-Z0-9_-]{0,63}$` label, its `none` sentinel and the ceiling the net believes it for. It is armed on EVERY delegation and not only under `auto=running`: one unconditional rule survives a compaction where a conditional the orchestrator must re-derive does not, and nothing reads the key without the `auto` marker beside it, so an attended run pays only the write. `platform/claude/plan.md`, `platform/claude/debug.md`, `platform/claude/quick.md` and `platform/claude/front-surface.md` point at that one statement instead of restating it — each naming only the launches its own flow makes, which is the whole of what differed between them — and `platform/claude/reporting.md` names the marker as what the net reads where it used to push.
- **The neutral bodies stop asserting a host fact.** The plan body required a report read "in the same turn" and claimed that "N launches in a single message all return in the same turn"; the debug body closed its wait rule with a clause disqualifying "a host that cannot make one wait" — which, read literally, disqualified Claude Code from running the loop this harness ships for it. What every host owes is one sentence: no step advances on a report nobody has read. HOW the report arrives — inside the launching turn, or as a notification that re-enters the conversation later — is the platform file's to state, where every other host fact already lives. `tools/hook-gates.txt`'s `SubagentStop` comment keeps its conclusion and loses the false reason under it: Claude still needs no receipt hook, because its own completion notification carries the report. `docs/parity-codex.md` corrects both rows this touched — the launch-wait row now reads as two mechanisms under one invariant, with the note that its previous `Settled` rested on a Claude flag that never existed, and the unattended-rails row names the hold beside the push.
- **The section that could go false is gated now, ceiling included.** It was ungated prose, which is why it stayed false for two releases. `tests/plugin-lint.sh`'s rule for it pins the section's clauses in the one Claude binding that states them and flags the retired flag by its own spelling anywhere under `plugin/skills`. The ceiling clause is DERIVED rather than typed: the rule reads `DELEGATION_WAIT_CEILING_MINUTES` out of the hook and requires that binding to name that number, so a ceiling moved in the shell and left alone in the text fails the rule instead of shipping — and a hook that states no single ceiling at all is its own flag rather than a silent pass. What that bar reaches is text and shell and nothing else, so FIELD ACCEPTANCE ships as a named pending: it cannot run until this release is installed, and no unattended run has yet been carried through the hold or the expiry. It is one run — resume the parked `wimm-web` roadmap child `plan04-categorias` and watch its slice launch an applier with no spurious push.
- **The record moves with it.** One decision — ADR-0150 (a delegation in flight is not a stall: the wait marker, its ceiling, and the invariant that is reading) — takes the log from 149 files to 150, and each of its three retirements is a CLAUSE named from both ends. ADR-0058 loses "the foreground requirement for applier and verifier is stated in both `plan` and `debug`", retired by a schema read rather than a change of mind, while the forked skills' `background: false` pin and the `model: opus` reasoning stand. ADR-0145 loses only Part 3's predicate — a third class of healthy turn-end exists — while its marker, its delivery carve-out entire, its byte-counted cap and above all its fail-open direction stand, the last of them EXTENDED to the new paths rather than moved, since the hold is itself an allowed stop. ADR-0148 loses two: "one that did not is a run being restarted into the same place", which the expired-mark class breaks, and "Two files, one location, one lifetime", which is three now that the wait sidecar joins the journal and the push tally under the same directory, mode and lifetime. `docs/blueprint.md` gains the index group and nothing else — its Mode 1 phase 6, phase 7 and Mode 3 phase 4 state the apply → verify loop at the design level and assert no host's delivery mechanism, so this change falsified no sentence in them and none was rewritten. `tests/plugin-lint.sh` grows from 41 rules to 42, and `tests/hooks-test.sh` closes at 1496 cases. `tools/hook-gates.txt` still carries 13 gate rows, `bootstrap/hook-hashes.txt` still publishes 14 hashes, and both plugin manifests read 0.25.0.

## 0.24.0

**Reinstall on both hosts — this release moves hook bytes, and one of the moved gates is Codex-wired.** On Codex run `bash bootstrap/install-codex.sh --yes`, restart, and re-trust through `/hooks`: `plugin/hooks/lib.sh`, `plugin/bin/oso-state`, the Codex hook manifest and the new production-boundary gate all changed, so `bootstrap/hook-hashes.txt` moved with them and grew from 13 published hashes to 14 — the installer also owns the block in `~/.codex/AGENTS.md` that gained this release's ceiling clause. On Claude Code `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code` carries the hooks, the contracts and the output style, and `bash bootstrap/install.sh` is owed on top of it this time — `bootstrap/claude-global.md` gained a clause and no plugin payload reaches the block it merges into `~/.claude/CLAUDE.md`. Every release through `v0.23.0` is published and tagged, so a machine on the newest published release crosses only this one.

A run the operator walked away from stopped at the first milestone it reported. The delivery contract had every operator-facing line END the turn, milestone reports included, and a turn that ends waits for somebody to say "continue" — which is exactly what an unattended run does not have. The order stood unconditional at every surface that carried it — both hosts' always-loaded global sources, the output style, the shared contracts, the plan body and every platform file that binds delivery — and it was right at all of them for as long as somebody was reading.

Underneath it two contracts were already unsatisfiable together and nothing had made them collide. The roadmap body has the chain arm its next child in the SAME BREATH as the close it reports, so that nothing waits on the operator; the Claude plan binding has operator-facing content END the turn with the tool call in a LATER turn. A milestone cannot both end the turn and share it with the arming that follows. With somebody present the contradiction costs a turn nobody notices; with nobody there it costs the run. This release closes that from both ends: the mechanics that keep such a run moving, and the contracts that now say what those mechanics enforce and how far they reach.

- **The Claude delivery contract carves out an unattended run.** While this repository's state carries `auto=running` for the session, milestone text does NOT end the turn — it rides the stream beside the work — and every milestone is ALSO appended full-text with `oso-state journal`, one journal per change. The trade is stated rather than hidden: the TUI may swallow text that precedes a tool call, so a milestone may never reach the screen, and the journal is what makes that a cosmetic loss instead of a lost report. The park and the run's own final report still end the turn, and the close is SEQUENCED so that report lands at all — `auto=done` first as a tool call, the report after it as the same turn's trailing text. `plugin/skills/_shared/reporting.md` states that a host's platform file may take the carve-out and names the journal that replaces the interrupted stream; `platform/claude/reporting.md` is the single Claude-side statement of it, with that host's plan, roadmap and debug files referencing it rather than copying it, and `plugin/output-styles/oso.md` and `bootstrap/claude-global.md` each gaining the one marker-conditioned clause that keeps a machine-global anchor from ordering the stall back.
- **The AUTO disposition is a marker on disk, not a ledger line alone.** Arming writes `oso-state set auto=running auto_change=<change-slug>`, a park writes `auto=parked` in the same write as the set-aside state — which is what tells a park from a stall without anybody inferring it — and the close writes `auto=done`. A roadmap's chain arms the same marker per CHILD, since a child of a chain is unattended by construction rather than by an operator's flip. The runtime triple is untouched and `mode=plan` throughout: the marker stands beside it and never over it, and the Claude plan platform file names the three hooks it arms — the `Stop` net, the `SessionStart` re-anchor after a compaction, and the production-boundary rail.
- **Three hooks read that marker, and the state binary gains the record they read.** `auto-continue.sh` is a `Stop` hook that blocks a stop and hands back a continuation order naming where the position lives, capped at three pushes that moved the run journal NOWHERE — progress measured in journal bytes rather than turns, because a turn only proves the model spoke — and it announces the give-up into the journal before it lets the stop through. Every failure path allows the stop: no session, no readable state, a marker that is not running, a session that does not match, a tally it cannot write. `reanchor-after-compact.sh` runs on `SessionStart` with `source=compact` alone and hands a fresh context the three places a run's position survives, arming for an unattended run OR any armed plan slice, since an attended run loses the same window to the same compaction; the four `SessionStart` handlers this plugin now wires stay disjoint by construction — the version notice is silent on `compact`, the stale-state notice fires only for a state file naming ANOTHER session where this one requires THIS one. `block-prod-deploy.sh` is a DENY-ONLY `PreToolUse` rail armed only while this session's own run is in flight: three vendor deploy CLIs recognized built in through the package runners that front them, whatever the repository itself names in `deploy-deny/<digest>.patterns`, and a `git push` that does not name the run's own branch — with an unreadable state file denying rather than passing for unmarked, and every denial naming the same way out, `auto=done`. And `oso-state journal "<text>"` appends the milestone full-text, timestamped and owner-only, to one file per change per repository under `runs/`. The event log was the obvious home and is wrong three times over: it cuts a command field at 120 bytes, it rotates on a 30-day mtime, and it is one machine-wide file with a size budget — so the highest-volume record would have gone into the file least able to hold it.
- **A compaction costs the window, never the position.** The roadmap body stopped claiming a compaction costs a chain nothing. How much window a client holds before it compacts is a setting the harness may ask for and can never guarantee, so the survivability floor is the host's own re-anchor, and what that re-anchor re-reads is what was written down: the `oso/index` row's `NEXT:` line, `oso-state show`, and the run journal.
- **An unattended run finishes at a pull request, and stops there.** AUTO's ceiling is contract now: the run reaches this machine and the environments the change's own bar reaches from it — local, and staging where the project has one — works on the `oso-run/<change>` branch §5 cuts beside the marker, and closes by pushing that branch and opening the PR for it. The never-solo list re-scopes around that and grows net: the change branch's push and the closing PR are the run's own finish, while a push to a SHARED branch, the PR MERGE, a release and — entering the list explicitly — a PRODUCTION deploy stay the operator's. Where no remote exists or the push or the PR call fails, the finish is a named pending in the final report rather than a silent skip or a retry loop. `bootstrap/claude-global.md`, `bootstrap/codex-global.md` and `plugin/output-styles/oso.md` carry the same boundary, the last of them now answering in the operator's place under an armed marker as well as inside a roadmap.
- **No delegated decision rides on "it is the standard".** The autonomy policy's standard-practice tier consults a current source wherever one is reachable — the docs route for a library, a framework or an API, official documentation otherwise — and CITES it in the delegated record; where none is reachable it writes the argument out instead, marked unsourced. The delegated record gains that basis as a field, so what an answer taken in the operator's absence rested on is readable when they audit it.
- **The per-project record stops claiming a reach it never had, and absorbs the ceiling.** `oso/preferences` was saved at `scope: personal` and described as honest per-machine. It never was: `mem_search` resolves the project from the working directory and filters by it UNCONDITIONALLY, and a scope argument only adds a filter on top of that — so a record saved in another project is unreachable from this one whatever scope it carried. One record PER PROJECT is therefore the design rather than a failure to remember, and it is now filled at TWO moments instead of one: the first plan in a project asks its two behavior questions, and the first AUTO or roadmap arming asks the three the ceiling rests on — the staging route, the production route, and the PR base branch. The split is the point: a project that never runs unattended is never asked for its production route. A legacy `scope: personal` copy found in that project migrates in place with every value kept, silently, because a record that already answers the questions is never a reason to re-ask them. The arming ask also writes the MIRROR the flow's own record cannot substitute for — the production answer distilled into one ERE per line under `deploy-deny/<digest>.patterns`, the file the production rail reads, created and appended through shell since no `oso-state` verb writes it, and REPORTED rather than skipped where it cannot be written. No record and nobody to ask arms no AUTO at all.
- **Codex gets the ceiling and not the continuity, and the parity ledger says so.** `block-prod-deploy.sh` is wired on both hosts and published in the hashes, so the boundary an unattended run may not cross is enforced identically. Neither the `Stop` net nor the compaction re-anchor is wired there, and `platform/codex/reporting.md` takes no carve-out — so the neutral contract's own closing sentence governs and every milestone on that host still ENDS THE TURN. What the run keeps is everything written down: the marker, the ceiling, the change branch and its PR finish, and the journal, since `oso-state journal` is the same binary verb on both. `docs/parity-codex.md` carries that as its own divergence row with the Codex net as a NAMED PENDING, and the AUTO row beside it stops claiming that nothing after the approval stops for the operator — no GATE does, which is what that row was always about, and the delivery rule is what does.
- **The Windows leg's abort class is swept to named skips, and the plan rail's decoded text is CRLF-normalized.** A native Windows `jq` writes its stdout in text mode, so every newline in a decoded field arrives CRLF while the shell takes back only the pair the value ends with — and the interior ones survived into both persisted plan artifacts. `json_field` now folds every CRLF pair back to one LF at the point where its two readers meet, so jq and the pattern reader still answer identically and the RAW escaped field the approval digest is taken over stays byte-untouched. That fold is a FIXED POINT rather than one pass: text mode re-terminates a line whose content already ended CRLF, so the byte run reaching the shell is `\r\r\n`, and bash's non-overlapping substitution leaves one `\r\n` standing for each pass it is given. The regression family carries that exact shape now, so a Linux run reproduces the residue a real Windows jq produces. On the test side the two exact-mode probes and the non-private-artifact pair carry named Windows-only skips — a host where `chmod` is a no-op can establish neither premise — the unarmed no-jq pair joins them, because its stripped-PATH simulation copies bash where bash's own libraries do not travel with the copy, and the `chmod` whose failure used to kill the whole run at 245 oks is a named FAIL now. The Codex Impeccable mount is laned there for a heavier reason: `mount-impeccable.sh` publishes its lock owner as a DANGLING symlink carrying the identity string — one atomic filesystem operation, and unimplementable where `ln -s` copies its source and a nonexistent source is an error, which is Git Bash's default — so every case resting on a completed mount is skipped under that reason instead of reported green. That is a KNOWN-OPEN product gap on Windows Git Bash, older than this release and hidden until the suite reached far enough to hit it; it stands for its own change and is not closed here. The first full Windows job is what says all of this: the eight failures the abort used to hide are gone, and the five it surfaced are these three classes. Three jobs in, one class had been paid for three times a lane at a time — the third run recorded no failure at all and then died at 127 on a copied interpreter, 40% through and short of its footer — so it is swept whole here by inspection instead of by the next red run: every fixture the suite builds with `ln -s` was read against Git Bash's default, and each one whose premise that default cannot express carries its own named skip. Those premises are three shapes of the same fact. A stub directory that IS a run's whole PATH cannot hold a working `cat` where the copy arrives without the libraries it runs from, which lanes the fallow, engram, context7 and impeccable wirings, the engram client-PATH probe and the Codex fallow resolver. A symlink is a claim ln-as-copy answers with a real directory and a real file — one an installer is then right to adopt — which lanes the symlinked hook directory and pre-commit, the symlinked agents directory and role file, and the two `.codex` escapes the Engram repair contract rests on. And a purge fixture's tree is made of a root link, a dangling link and a relative pointer, none of which that default can publish at all. Fourteen platform-keyed skips stand where the class was, ten of them new and covering 90 cases, each naming what it could not build and none of them silent; whether the leg now reaches `failed: 0` and its footer is what the NEXT Windows job says, because a Linux run under forced platform flags cannot stand in for one — the unfixed tree closes green under those same flags, since forcing them does not reproduce ln-as-copy. One fixture in that census went the other way, because its premise Git Bash CAN build: the Codex `SubagentStop` runtime-fallback PATH — the farm of seventeen linked coreutils whose copied `cat` is what took the run down — is now the same plain system PATH the plan rail beside it has always used, so that rail is measured on Windows rather than skipped there, and the fixture it shares with the plan rail is spelled once. The Impeccable mount's own uniqueness primitive moved with it: it published its owner as a DANGLING SYMLINK carrying the identity string, atomic because `ln -s` never replaces a path — and Git Bash's `ln -s` COPIES, so a source that does not exist cannot be copied and the publication failed on that host alone. It now claims a unique DIRECTORY with `mkdir` and writes the identity inside it: the same one-operation uniqueness, expressible everywhere. The Codex installer's happy path runs that mount, so its failure rolled the whole install back and every assertion reading a completed install failed behind it; the lane those cases stood in comes off with the idiom that justified it, and `publish_git_bash_path` normalizes its candidate through `cygpath` before storing and comparing, the shape its sibling `publish_state_bin_path` already had — a raw store read back through a native `jq` that rewrote it reported FAILED for a write that had in fact landed.
- **The record moves with it.** Five decisions — ADR-0145 (the unattended run: the delivery carve-out, the marker and the Stop net), ADR-0146 (the context economy: a bounded window and a re-anchor floor), ADR-0147 (the AUTO ceiling: local and staging, the PR finish, production never), ADR-0148 (the run journal) and ADR-0149 (the per-project record: honest scope, absorbed ceiling) — take the log from 144 files to 149, and every retirement is a CLAUSE named from both ends rather than a decision replaced. ADR-0145 retires ADR-0144's "no fourth key and no gate that reads one" by that decision's own test — it rejected a key because nothing would read one, and three hooks now do — along with the unconditional standing of ADR-0032 and of ADR-0115's delivery section, for a milestone under a carve-out and nowhere else. ADR-0146 retires ADR-0138's "a compaction costs it nothing": the position half was right and still is, and the window was the half nobody priced. ADR-0147 grows ADR-0136's never-solo list from four to five under the growth clause that decision wrote for itself, re-scopes its first item, and takes ADR-0093's push-and-PR absolute down to the run with an operator at it. ADR-0149 retires ADR-0007's `scope: personal` field, the per-machine framing ADR-0025's context rests on — that decision's own conclusion stands entire and holds more strongly at the narrower scope — and ADR-0030's "asked once, never re-asked", while the round it defines stays at two questions. `docs/blueprint.md` gains the index group and moves where this change made it false: phase 0's record read names one record per project with the ceiling fields asked at a different moment, phase 7's close conditions the push and the PR on the operator being at the run — theirs to ask for while they are here, the run's own finish where they are not — while the PR merge, a release, a production deploy and an accepted security residual stay theirs on every route, and Mode 4's policy says five rather than four. `docs/parity-codex.md` gains the rails row and corrects the AUTO row beside it. `tests/plugin-lint.sh` grows from 37 rules to 40 — one pinning the carve-out's own clauses in the Claude reporting binding, all three of the marker's values in the plan body's AUTO ground rule, and the compaction economy in the roadmap body's chain phase, where the retired absolute is flagged if it comes back; one pinning the ceiling — the re-scoped never-solo items and the finish beside them, the standard tier's citation duty, the close's own push and PR with the pending it parks, and the blocked exit that flips the marker to `auto=parked` rather than leaving an intended stop reading running; and one pinning the per-project record — the one-record-per-project scope with the retrieval fact behind it, the two moments that fill it, the legacy `scope: personal` copy it migrates instead of re-asking, and the arming ask's deny-pattern mirror, with the retired per-machine claim flagged if it returns to the plan body. `tests/hooks-test.sh` closes at 1482 cases, `tools/hook-gates.txt` goes from 10 gate rows to 13 — two of the three wired on Claude alone — and both plugin manifests read 0.24.0.

## 0.23.0

**Reinstall on Codex, plugin update on Claude Code, and no re-trust anywhere — this release touches zero hook bytes.** What it moves is prose and the linter that gates it: two flow bodies, three shared contracts (the rubric, the front-surface trigger, the milestone reporting contract), both hosts' `oso-verifier` role, `tests/plugin-lint.sh` and the mutation cases behind its four new rules. On Codex the installer owns two of those surfaces — it stages `plugin/skills/_shared` into the private marketplace it owns AND copies the rewritten role out of `codex/agents/` — so run `bash bootstrap/install-codex.sh --yes` and restart Codex; the installed hooks and the `bootstrap/hook-hashes.txt` they are verified against are byte-identical to 0.22.0, so `/hooks` needs nothing from you this time. On Claude Code `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code` carries it whole, the agent role included, refresh first for the reason 0.21.0's version-notice bullet gives: the marketplace entry's source is `./plugin`, and every runtime file this release touches lives inside it. `bootstrap/claude-global.md` is untouched, so no `bash bootstrap/install.sh` is owed for this one. Everything from 0.20.0 on is still unpublished — `git tag` reaches only `v0.19.0` — so a machine coming from the newest published release also crosses those releases and owes their remedies.

A verifier failed a slice for adding inline comments, correctly, and the orchestrator overruled the verdict in the payload that relaunched the applier — then wrote a standing ruling into every later verifier payload telling the next judge not to fail on that rule again. The ban the verdict rested on ships at three layers, and all three held: the rubric states the class, the applier is told before it writes, the per-slice gate fails a diff that adds one. What none of them bound is the ROUTER between them — the orchestrator that reads a verdict and builds the next payload — so the fourth layer goes there.

What that layer makes structurally impossible is the reach, not the mistake. An orchestrator can still be wrong about a finding; what it can no longer do is write its wrongness into the instrument that judges everything after it, because the payload is now governed at both ends — the routes state what it must carry, and the judge refuses what it may not. The three defenses are deliberately not redundant: a route bound alone still meets judges that honor anything in front of them, a judge bound alone still costs a full round every time it refuses, and neither reaches the case where the repo's own habit is the argument — which is why the rubric now ranks its four no-exception rules above the conventions of the code under judgment, and sends that conflict to the operator during planning instead of to a gate nobody is standing at.

The release's other half is the property a roadmap had and a single change did not. `/plan` gains an AUTO disposition: one more question in a round it already asks, and the operator can leave. It is not a fifth mode, not an invocation argument and not a runtime key — it is a dated line in the change's own ledger, flippable either way by a plain instruction at any point, and everything it turns on was already written for an operator who is not there.

- **The three routes that send a writer back carry the verdict verbatim.** `bodies/plan.md`'s sequential `fail` route, its red-slice route in the wave loop and `bodies/debug.md`'s verifier `fail` route each relaunched the applier "carrying the verifier's findings" and said nothing about what carrying one means, while the debt-cleanup handoff beside them had that discipline written out in full. Each now carries every finding with its `file:line` and the evidence the verdict recorded for it, never the orchestrator's summary of them, which is how a relaunch ends up answering a finding nobody made. The design audit's own fix route in `plugin/skills/_shared/front-surface.md` gains the same requirement, matching the handoff it mirrors.
- **A finding grounded in a no-exception rule has two routes, and neither of them is overruling it.** The rubric names exactly four rules its own judgment contract may not override — the three Hard blockers and the inline-comment debt class — and a verdict that failed on one of them is settled: never overruled, filtered, softened or reinterpreted, and no payload the orchestrator builds may instruct any judge away from one of those rules, which is the standing ruling closed at its source rather than at the one payload that carried it. What is left is FIX, the relaunch carrying the findings verbatim, or ESCALATE, where the operator picks with options and tradeoffs in front of them; under a roadmap ESCALATE is the reconciliation-class question those ground rules already queue, with the change set aside on it. Two carve-outs stand beside the rule so the absolutes do not collide: the doubt-pass reconciliation stays the orchestrator's own, since that judge runs pre-freeze, is advisory and grounds nothing in a no-exception rule, and the sweep's disposition tags still travel bare, since this binds verdict AUTHORITY and never information flow generally. A new linter rule asserts both clauses at each of the four routes.
- **The verifier's payload is a closed list, and its comment gate is a scan it has to show.** Both hosts' contracts enumerated what the orchestrator hands over and neither said the enumeration was exhaustive — and an enumeration that does not say so reads as a floor, which is precisely how a standing ruling arrives as extra instruction to honor. The list is closed by its own sentence now, with the three shapes that arrive past it named: a standing ruling, a project convention offered as an input, any instruction that softens a gate or pre-judges a criterion. Two clauses keep the closure honest rather than self-defeating — Codex's `HANDOFF SLICE`/`HANDOFF ATTEMPT` envelope is carved out by name, since it is transport and never an assignment field, and the ledger decisions the unledgered-abstraction gate cannot run without are named INSIDE the legitimate set, because they were already being sent and closing a list that omits them would refuse a correct payload. A project's conventions are an applier input and never a verifier one: the gates judge against the rubric alone. `blocked` gains a fourth trigger for the refusal — "I will not" beside the three "I cannot"s — admitted at every site that enumerates a cause, which on Claude Code means both report shapes' `reason:` lines as well as the closing definition, because an agent filling in a field reads the line describing it and not a definition four hundred words back. The gate that failure was fought over stops being a reading in the same pass: "fail the slice if its diff ADDS an inline comment" is satisfiable by reading it, by skimming it, and by never looking — and the three are indistinguishable in a verdict, which is why eighty-seven comment lines can land under greens nobody can audit after the fact. Both contracts now RUN a scan of the slice diff for added lines that open a comment, CITE its command and output as verdict evidence in the same shape the rest of the evidence takes, and judge every hit: the banned class fails, and only a hit shown to be the language's standard public-API doc form stands. No pattern is pinned in the contract — the gate is language-generic and a `//`-and-`#` pattern would teach a judge that those two are the class — so what is pinned is that a scan ran and that a reader can see it. A verdict on a slice whose diff was never scanned is one neither contract accepts.
- **The four no-exception rules outrank the conventions of the repo under judgment.** The rubric told a change to follow the codebase's existing pattern for a concern, and required every finding to name a concrete readability win — two clauses that, over a repo whose every handler swallows errors or whose every file writes comments, hand the argument to the defect: the code IS consistent, and consistency is a readability argument. The judgment contract gains the ranking as a bullet and "One style per concern" gains the bound in its own sentence, because stating it once would have left the other clause reading exactly as before. A conflicting pattern is EVIDENCE about that repo, never a standing exception, and the conflict is a question for the operator. It is asked where it is cheapest: surface mapping audits its map a SECOND time against the four and any tension becomes a battery question, ranked with the rest, answered in decision rounds and recorded in the frozen ledger — one question in a round the operator is already sitting in, rather than a red slice and an escalation after a writer has already written what the convention asked for. The escalate route names that conflict as its canonical case with both readings the operator picks between: keep the class's bar by fixing the slice, or amend the ledger explicitly, which is theirs alone to do.
- **`/plan` gains an AUTO disposition, and parks on what the policy will not answer.** It is asked as the second question of the execution-mode round — normal the default, since auto buys nothing for an operator who is sitting there — or flipped by a plain instruction at any point, mid-execution included, both ways, each flip dated in the change's own ledger, and the flip itself is the consent: a confirmation round in front of the one action whose purpose is that you are leaving would defeat it. Planning always runs with you; auto governs execution and close alone. What answers in your place is the roadmap's own autonomy policy, named rather than copied and read at one change's scale — three tiers, irreversibility bar and never-solo list entire, with this change's frozen ledger answering first where that policy reads two. Everywhere the plan body says "under a roadmap" an auto run reads the same, with exactly two substitutions: what a child queues for the roadmap's presence phase, this run queues for its own final report, and where the chain would arm the next child, this run PARKS — a blocked report carrying the questions as they stood, the full account beside it, the set-aside state written, and the tree left exactly as the stopped work left it. A park is an end of the turn and never an abandonment: the position stays durable in the ledger, the plan topic and the index row, and the resume is the mode's own re-invocation. A message does not disarm it — a queued answer is consumed and the run continues, only an explicit resume hands it back — so neither silent failure stands, auto dying on a glance nor your return going unheard. No new mode, no invocation argument, no fourth runtime key: `mode=plan` throughout. The reporting contract's second exception to its length bound generalizes with it, from a roadmap's final report to an ABSENT-OPERATOR run's, naming both runs that produce one; the list of exceptions is still closed at two.
- **The record moves with it.** Four decisions — ADR-0141 (fail routes carry a verdict's findings verbatim, and never overrule a no-exception finding), ADR-0142 (the verifier's payload is a closed list, and its inline-comment gate is empirical), ADR-0143 (the four no-exception rules outrank the conventions of the repo under judgment) and ADR-0144 (a plain plan runs under its own AUTO disposition, and parks on what the policy will not answer) — take the log from 140 files to 144, and not one of them retires a clause of anything: no `Supersedes:` and no `Superseded-by:` pair is written by this release, which is stated where it could be doubted. ADR-0141 COMPLETES ADR-0134 rather than replacing any part of it — that decision's three layers all held in the field failure, and the router between them was never bound — and ADR-0144 folds the reporting generalization in rather than filing it alone, on the test that it can neither be reverted while auto stands nor stated without it; ADR-0139's own exception reads exactly as it did, now with a second bearer under a wider heading. `docs/blueprint.md` gains the index group and moves where this change made it false: Mode 1 phase 4's round is two questions and records three things, phase 3's "returns to the human" absolute names the auto run beside the roadmap with its two substitutions, and phase 7's close carries the machine entry condition for both plus the one report an auto run owes at whichever end it reached. README squares the same claims — the plan bullet gains the disposition and the one honest sentence about both hosts, the reporting clause generalizes with the contract, and the first design principle stops saying ONE case answers in the operator's place when there are now two, each declared before it is used. `docs/parity-codex.md` gains the row this earns: a plan run under auto is unattended on BOTH hosts, because its one approval is the plan gate each host already has and that gate is per-change — the ASSISTED standing in the loss ledger is a roadmap fact about a queue whose every child meets that prompt again, and it does not reach a single plan. Both plugin manifests still say four modes and the Claude marketplace catalog still names four in both of its descriptions — the Codex catalog carries none to say it with — because auto is a disposition of one mode and not a fifth; neither catalog carries a version field, and both manifests read 0.23.0. `tests/plugin-lint.sh` grows from 33 rules to 37, and `tests/hooks-test.sh` closes at 1398 cases.

## 0.22.0

**Reinstall required on Codex; on Claude Code the plugin tier carries the mode and the reinstall carries one line.** The Codex side is the installer's whole: it stages `codex/skills/` and `plugin/skills/_shared` into the private marketplace it owns, it places the two hooks whose bytes moved here and verifies them against `bootstrap/hook-hashes.txt`, which moved with them, and it owns the block in `~/.codex/AGENTS.md` that gained the routing line — so run `bash bootstrap/install-codex.sh --yes`, restart Codex, and re-trust the hooks through `/hooks` before starting a thread. On Claude Code `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code` carries the mode WHOLE and usable, refresh first for the reason 0.21.0's version-notice bullet gives: the marketplace entry's source is `./plugin`, and the wrapper, the neutral body, both platform files, the two moved hooks and the output style all live inside it, while the marketplace manifest's own description arrives with the refresh. What no plugin update can carry is `bootstrap/claude-global.md` — `install.sh` merges it into a marked block in `~/.claude/CLAUDE.md`, a file no plugin payload reaches — and the one line it gained is what makes the harness OFFER roadmap when several substantial changes arrive together. So `bash bootstrap/install.sh` is what completes the release there, and until it runs the mode works when you type it and never gets suggested to you. Both crossings sit on top of two releases nobody has published: `git tag` still reaches only `v0.19.0`, so a machine coming from the newest published release also crosses 0.20.0's `bootstrap/` work and 0.21.0's Codex reinstall and owes their remedies too. This release keeps its own entry rather than folding into 0.21.0, on the test 0.20.0 already used against 0.21.0: that entry's subject is a clean-code bar, its remedy says the Claude plugin tier carries it whole, and its acceptance was a cold read-back of a rubric — none of the three is true of a fourth mode whose acceptance is the first real roadmap somebody runs.

An operator with a batch of substantial changes already decided had to decide them again one plan at a time. `/plan` asks for the intent, the decision rounds and the approval once per change, and when the batch was settled together most of those answers are the same answers: one intent approval per change, one set of rounds per change re-answering the last set, one plan approval per change, and a human standing at every gate because each of that flow's gates is written for somebody who is there. The mode below pulls the deciding forward into one sitting and then runs the queue on what that sitting recorded.

- **A fourth mode runs an ordered queue of plan changes on one approval.** `/oso-code:roadmap` on Claude Code, `$oso-code:roadmap` after native `/plan` on Codex, operator-only on both. Its exchange is three phases — the children in run order with each one's intent, a GLOBAL LEDGER whose entries name the children they bind, and each child's own decisions — and one approval then covers the planning behind that queue and the execution of every child in it. The approval names what it does not buy as plainly as what it does: a queue that changed materially after presentation goes back through the gate whole, every child still builds and delivers the Repaso-headed plan document, and a decision the policy may not take is not licensed by it. A child is a PLAN-mode change and nothing else is queued; a quick or debug child is a recorded YAGNI rather than a silent omission, because each would need answers this version settles for plan alone — what one approval covers for a mode that has no plan document to approve, and what a debug child does when no reproduction can be got without the operator. Codex ships it ASSISTED, not unattended: its approval rail passes only on the byte-exact operator prompt, which no model may produce on anyone's behalf, so the chain advances by itself and each child still stops there. That is a release loss and it went in the parity ledger rather than a footnote — along with the fact that the mode's own Plan-Mode precondition is backed by no hook at all, since the gate that reads an invocation spelling reads plan's alone.
- **A policy declared before the approval is what answers a decision after it.** An unattended chain guarantees that one surfaces rather than risking it, so what answers it is agreed to in advance instead of improvised later. Two outcomes and no third: resolved and recorded as DELEGATED naming the tier, or QUEUED for the end while the chain moves on. Three tiers, each reached only when the one above has nothing to say — the recommendation the flow's own rounds were already required to produce, then what current practice standardizes, then the simplest thing for the operator to live with — and a tier that could not justify its answer in those same terms stays silent rather than dressing a coin toss as a recommendation. The safety net is one question: does undoing this need more than an edit and a re-run in this repository? A restore, a refund, a revocation, a retraction or another person's involvement puts it over the bar, and over the bar the policy defers instead of deciding. Four things are never a tier's question at all — a push or release, a ledger amendment, an accepted security residual, a forced deletion — because each is already refused where its own flow states it.
- **Four gates of the plan flow gain an exit, and a queued decision costs one child rather than the queue.** That flow is written for an operator standing at each of its gates; with none, four of them had no reachable exit and a child would have sat at one instead of reaching an outcome a chain can act on. Where the policy can answer — the applier's blocked questions, a conformance finding whose CODE diverged, a pick among the sweep's three cap routes — it answers in the operator's place and the answer lands in that change's own ledger, which is what makes the relaunch a fresh applier over a ledger that MOVED. Where it will not, the question is queued and the change is SET ASIDE where it stands: committed slices stay committed, an applier's pending edits stay in the tree, a conflicted merge stays as git left it, and only what the stopped work armed is disarmed. The close gains a machine entry condition in debug's shape, because nobody is there to say they are happy — the last slice green and marked, or the last wave's gate passed — and it is an entry and never a permission: the push, the PR and an accepted security residual stay the operator's. Both texts that send a decision to the operator keep that instruction word for word and gain the one case that answers instead, which the operator approved before leaving.
- **The chain refuses to arm a child over a tree that is not its own.** A child is armed with what the queue recorded, run to one of the only two words the chain reads, and the next one released on either. Before every arming it reads the checkout — `git status --porcelain`, EMPTY the bar, an untracked file included, because the next child's `add -A` takes one exactly as it takes a modification — and it reads the WORKTREE ROOT beside it, because a child set aside mid-wave dirties the checkout nowhere while its survivors sit on the very paths the next child's first wave cuts, same slice numbers, no child anywhere in the name. Over a dirty tree it arms nothing further: every un-run child takes the set-aside word with its reason, the runtime key is disarmed, and it all reaches the presence phase. Discarding the work is over the policy's own irreversibility bar, forcing a removal is on its never-solo list, and committing it under either child is an attribution nobody asked for, so refusing is the only honest move left. A roadmap in flight is durable rather than remembered: its ledger and its position are engram's, and a `roadmap` runtime key that arms no gate — so a write that never landed costs recoverability and never safety — ships with the SessionEnd sweep that drops it and the SessionStart signal that names the resume route.
- **Everything the chain could not do lands in one ordered queue behind one report.** Every decision the policy would not answer, every child a decision set aside — as ONE item with the decision, not two — whatever stopped the chain along with the standing worktree only the operator can dispose of, and every child's own pendings and named residuals, closed children included. The order is a rule rather than a matter of taste, so the same inputs produce the same queue twice: what stopped the chain, then what a child waits on, then what a running child needs, then what can be read at leisure, ties broken by run order and then by the point inside the child. The report heads it and answers three things — what was decided in their absence and on what rationale, what was deferred and why, what waited for their hand. Their run stops in front of them at three moments at most: the roadmap finished, the queue is ready, the chain is blocked. A child closing is deliberately not one of them, and the reporting contract gains that disposition as a sixth milestone which reaches the session stream alone — plus the one exception the final report needs, since a queue and its residuals do not fit in three lines.
- **The harness stops exempting its own linter from the comment ban it exports.** ADR-0132 and ADR-0134 installed that ban and then carved one file out of it by name: `tests/plugin-lint.sh` documented a reason above every rule, and its header claimed exactly that. Two changes later the carve-out stood over 746 comment lines, and the header's claim was already false for the rule that had landed without one — a bar that holds everywhere except in the file documenting the bar is not a bar, and a header claiming a convention its own file had broken is the false claim 0.21.0's clean-code bar went after. The convention is retired, the carve-out is gone from both ends, and the file keeps a two-line header naming the count and nothing else. What replaces the reasons is not a shorter comment: ADR-0140 holds the ceilings, the shapes a pattern deliberately does not catch and the near-misses somebody measured — the part no name and no test can state — while the flag messages and function names carry what the code says itself, and `tests/hooks-test.sh`'s per-rule mutation cases carry what each rule actually catches. No rule's behaviour moved with it: not a locator, not a pattern, not a marker set, not the registration order, not the declared count of thirty-three across its four surfaces. The purge stops at `plugin/hooks/`, `bootstrap/` and `tools/`, whose bytes are published as hashes and one of which is a Codex trust boundary, so an edit there would cost a hash roll and a re-trust for a readability win; inside the two test files it is scoped to what those two changes ADDED, so `tests/hooks-test.sh` keeps every `# ---` divider and an older block stands visible rather than half-swept.
- **The record moves with it.** Six decisions — ADR-0135 (the mode and its one approval), ADR-0136 (the autonomy policy), ADR-0137 (the set-aside disposition), ADR-0138 (the chain and its tree bar), ADR-0139 (the presence phase) and ADR-0140 (the linter's retired rationale convention) — take the log from 134 files to 140, and each retirement is named from both ends: ADR-0139 retires two clauses of ADR-0115, its five-milestone enumeration and its one-exception rule, while that decision's required facts, its length bound, its per-host platform binding and its single-definition rule all stand; ADR-0140 retires the Part 3 of ADR-0132 and of ADR-0134 alone — the exemption each granted one file by name — while the citation ban with its file set and its notations, and the debt class with its three layers and its doc-form carve-out, stand exactly as those two record them. `docs/blueprint.md` gains Mode 4 and moves where this change made it false — Mode 1 §3's "returns to the human" absolute, §7's close entry, the planning line's "entirely in the host's native Plan Mode" absolute, and the engram row that had every `oso/index` row going `executing` → `done` when a roadmap's own row is a parent marker that stays. README gains the mode, its per-host spelling, the roadmap in the model expectation, the resume route for a state file naming one, and the one case its first design principle now allows; its second design principle gains the same condition, because one approval on Claude Code is the transition out of Plan Mode as much as the approval and a child that entered again would owe a second one — so a child's phases 1–5 run outside that mode, read-only by the plan flow's own rule. That fact is stated once, in the Claude roadmap platform file; the plan body's first ground rule and the Claude plan platform file send a child there rather than to a gate the roadmap already passed, the front-surface pin step rests on the flow's own rule instead of the host's mode, and the parity ledger's approval-gate row records what renders and approves a child's document on each host. Both plugin manifests and both marketplace descriptions stop saying three modes; the Codex manifest's three starter prompts stay three, the maximum that spec allows. `tests/plugin-lint.sh` grows from 28 rules to 33, and `tests/hooks-test.sh` closes at 1394 cases.
- **Three things ship known-open rather than found later.** Behavioural acceptance of this mode is the operator's FIRST REAL ROADMAP RUN and nothing here stands in for it: the bar this closes at judges text and shell, never a queue of real changes carried unattended, so the acceptance is explicitly pending. One of the presence phase's three producers of operator-hand work has no mechanism — a slice DECLARED as the operator's own, where `bodies/plan.md` §4 states exactly four slice fields and none can mark one that way, so the class ships with its two working producers and the gap recorded in ADR-0139 instead of implied by a body that lists three. And `check_milestone_reporting_contract_is_complete` has a pre-existing path to a silent green, now attributed rather than merely observed: its mode loop derives an operator-only mode from frontmatter, `frontmatter()` returns empty and exits 0 when the read of line 1 fails, and the loop then `continue`s past that mode with no flag — the mechanism behind the one silent green its `debug.md` mutation case produced in roughly forty runs. `check_global_routing_names_every_operator_only_mode` shares the shape. Fixing it is its own change and it is not attempted here.

## 0.21.0

**Reinstall required** — on Codex, because the installer owns everything this release changed there: `bootstrap/install-codex.sh` copies the three rewritten agent roles out of `codex/agents/`, stages `plugin/skills/_shared` into the Codex plugin, and places the hooks it verifies against `bootstrap/hook-hashes.txt`, which moved with their bytes. Run `bash bootstrap/install-codex.sh --yes`, restart Codex, and re-trust the hooks through `/hooks` before starting a thread. On Claude Code the plugin tier carries this release whole — the marketplace entry's source is `./plugin`, and every Claude runtime artifact this release moved is inside it; outside it the `bootstrap/` scripts moved comment-only, and the rest is the Codex side, the record and the bar — so `claude plugin marketplace update oso-code && claude plugin update oso-code@oso-code`, refresh first for the reason the version-notice bullet below gives. What no host escapes is 0.20.0: it is committed and unpublished, `git tag` still reaches only `v0.19.0`, so a machine coming from the newest published release crosses that release's `bootstrap/` work as well and needs its reinstall too. 0.20.0 keeps its own entry rather than absorbing this one — its remedy and its acceptance gate are about a Windows install, and none of this is.

The harness exported a clean-code bar it did not run, and taught a notation it forbade. Its rubric banned over-documentation and then handed the exception back: an "Earned WHY" example blessing a comment that carries an external constraint the code cannot show, which a frozen ledger decision wears exactly. The applier — the agent that writes every line of code — had never been told the rule at all; its contract enumerated four of the rubric's five sections and skipped the one carrying it. The per-slice verifier's whole rubric mandate was the Hard blockers, and over-documentation lives in Debt markers, so a change executed as N slices passed N gates that structurally could not fail one for a comment. The debt sweep meant to catch the rest ran a judge → fix loop with no exit. And this repo's own shell carried 155 decision citations behind a linter rule that made the notation look like a checked contract worth copying — so an applier copied it, into a customer's TypeScript, where the ledger those tags name never existed for the reader at all.

- **The inline comment is a debt class with no exception, and the writer is told before it writes.** The rubric states it with no exceptions and in the Hard blockers' own phrase, so the judgment contract's readability-win requirement cannot spare an accurate citation; only a language's standard public-API doc form stands outside the ban, and provenance goes to the change's ledger, the PR body or the commit message. Both hosts' `oso-applier` contracts now map all five of the rubric's sections, Debt markers included, state at write time that the inline comment is not a thing the applier produces, and route decision ids and rationale to the report's `decisions_used` field, where a citation is debt however accurate it is. `/quick` binds its own writer the same way, since the orchestrator is the writer there. Read back cold — a fresh judge given the shipped rubric, eight real comment samples from that same PR and none of the reasoning behind either — the text returned seven debt, one earned and nothing unclear: the earned one a JSDoc block on a banker's-rounding helper, the doc form the rule keeps, and among the debt the vendor-API pagination comment that used to BE the rubric's own `Earned WHY` exemplar. One judgment on eight lines against a text, not a re-run of the harness over that PR.
- **The slice gate can see a comment, and the conformance axis stops rewarding one.** Both hosts' `oso-verifier` contracts fail a diff that ADDS an inline comment, beside the Hard blockers rather than in place of them, judging the added lines and leaving the rest of Debt markers to the sweep so the gate does not become the unbounded auditor. The conformance axis had the opposite problem: it called an unimplemented decision one with no trace in the diff, and a line reading `// D22` is the cheapest trace there is, while scope-creep closed the other exit. A trace is now the code that enacts a decision; a comment naming one lands in the debt section and leaves the decision unimplemented, which is what the two separate sections were for.
- **The sweep's judge → fix loop gets an exit it cannot run past.** The close said loop judge to fix until clean, and a real close took seven rounds over three hours in which rounds five through seven found nothing but the damage round five's own applier had caused. The loop now ends on a band — no blocker and no structural finding open — and a nit that survives is the named residual: recorded in the ledger and relayed to the operator verbatim under an explicit exception to the reporting contract's counts-only rule and its length bound, never dropped because the loop was allowed to end. Three rounds is the cap, and at the cap the operator picks from exactly three routes. `/debug` takes the same bar on its additive sweep, and only there: the quality-pass loop it chains into is a different judge and stays uncapped. Every surface that stated the retired condition as a live precondition moved with it, including the design audit's, which would otherwise have demanded a verdict the new bar can never produce.
- **That loop carries its own memory, and its cleanup sweeps a class inside a boundary.** Every round ran blind over the whole diff and no rule forbade re-raising, so an orchestrator hand-carried a do-not-re-raise list that grew from two items to fourteen while one clone group was dismissed three times. The re-invocation now carries the prior rounds' findings with their dispositions — the tag, never why it became one — and a dispositioned finding is named as settled instead of argued again. The handoff that runs in that loop had no payload spec at all while the judge-findings kind beside it had one, and now gains the same: findings verbatim with file, line and tier, the change-surface list, the rubric path. Its report gains a line per finding, because a file-keyed list cannot say whether finding four was fixed or skipped. And the cleanup sweeps the pattern rather than the instance, which is what made an N-site defect cost N rounds — bounded by the findings and the change surface, per pattern and never a whole-file pass, because the inverse failure is a formatter run over documents the change touched in one cell.
- **The harness stops exporting the convention it now bans.** All 156 citations are gone from every executable — the 155 this repo's shell carried, plus one more in `bootstrap/verify.bat`, whose `REM` comment no rule scanning shell for a `#` could ever have matched — and `Implemented-in:` went with them from every decision file that carried it — 63 at the moment the purge ran, which is the 61 the log already held when this release opened plus the two it wrote under the convention before retiring it, so a diff read against that opening point sees only 61 removals. The plan body's verification-bar-coupling exemplar teaches the same lesson through the rule-count relation, which couples the linter and README exactly the way the retired example coupled a skill and a decision. Markdown prose keeps its cross-references: a document citing its decision is not a comment in code. A new rule scans every executable this repo ships — the six shell sets the syntax gate parses, and the Windows entry points and awk program it cannot — in the four notations this repo writes and the three markers those languages open a comment with, and the narrowed resolve check stays, because the prose references the decision preserved would otherwise be guarded by nothing. `tests/plugin-lint.sh` grows from 22 rules to 28, and `tests/hooks-test.sh` closes at 1366 cases.
- **A session says once when its plugin is behind the published release.** The operator's machine ran 0.17.0 for five days while 0.19.0 was published and 0.20.0 prepared, so every improvement this harness shipped sat unreachable in their projects and nothing said the manual refresh was overdue. A `SessionStart` gate now reads the version from the plugin directory it launched from, requires the marketplace registered on the machine to be served from that plugin's own repository, and reads the newest release tag off the git ref advertisement under curl's own connect and total bounds, cached for a day so an offline machine tries once rather than once per start. On drift it prints one line: both versions and the two commands that close the gap. It is silent on everything else — no drift, no network, no curl, an unreadable version, a foreign marketplace, a compaction. Claude only: Codex installs from a clone into a runtime directory no `claude plugin` command reaches, so a Codex-wired handler could only print a remedy that does not apply there. README documents the marketplace refresh beside the update, because an update alone reinstalls whatever the client's stale clone already holds — which is exactly how 0.17.0 survived.
- **The record moves with it.** Five decisions — ADR-0130 (the sweep's banded, capped exit), ADR-0131 (the loop's memory and the cleanup's class sweep), ADR-0132 (citations leave executable code), ADR-0133 (the version notice) and ADR-0134 (the inline comment as a debt class, held at three layers) — take the log from 129 files to 134, and each retirement is named from both ends: ADR-0130 retires ADR-0071's until-clean clause, ADR-0133 retires ADR-0075's single-command update spelling, and the rest of both still stands. ADR-0134 retires nothing, and says so rather than implying a predecessor: no decision ever recorded the over-documentation rule or the escape hatch this one removes — the rule arrived in a `fix(quality)` commit that amended the blueprint nowhere, leaving this log nothing to extract for it, and the `Earned WHY` example rode in beside ADR-0005 without appearing among the four clauses that decision records. `docs/blueprint.md` moves where this change made it false — Mode 1 §7's close states the band, the cap and the three routes, and the Decisions preamble stops listing the retired citation field among what a decision file carries. README's debt-sweep row stops promising to judge "stray comments", a phrase that appeared in no executable here, and names the over-documentation class the rubric now states.

## 0.20.0

**Reinstall required** — `bootstrap/` changed almost everywhere, and no plugin update carries it: pull the release and re-run `bash bootstrap/install.sh` (double-click `bootstrap\install.bat` on Windows), then restart Claude Code. The run publishes two new `settings.json` `env` keys and provisions two binaries it previously only reported on, so an existing install that looks healthy is exactly the case this release is for.

0.10.0 claimed the harness "installs clean on a fresh Windows box and stays wired". Measured on a real one, almost every layer of that was reporting success over work it had not done: a carriage return split the state file the commit gate reads, the installer compared its own `core.hooksPath` against itself and lost, `HOME` pointed at a tree the client never opens, the engram binary the summary reported OK had never been installed or probed, fallow needed a Rust toolchain nothing provisions, the backup announced in phase 1 did not exist until phase 6 and held the run's own work when it did, and the only way to check any of it was a terminal the same page promised you would not need. Every one of those went green. This release closes the class rather than the instances, and states what is actually required instead of "nothing pre-installed":

- **A carriage return no longer splits state, cleanup, and `CLAUDE.md`.** `json_field` captured jq output through `$(...)`, which strips a trailing newline and never a trailing CR, so four gates fed a CR-bearing cwd into the state filename digest — one session wrote `verify_green=true` into one file while the commit gate read another. Both readers strip it now, the pure-bash fallback included, and `state_file_for` strips at the choke point too, where a CR makes `git -C` fail and drops the digest onto a directory nobody named. `gentle-manifest.txt` and `claude-global.md` are LF-pinned and all three manifest readers strip defensively, because `.gitattributes` renormalizes no clone that already exists — with a trailing CR the legacy cleanup removed nothing and `verify.sh` confirmed it as `ok: legacy artifacts removed (0)`. And `merge_global_claude_md` compares its managed-block markers CR-tolerantly, so a re-run stops appending a second copy of a block it could no longer recognize and then blaming the operator for the bloat.
- **Windows paths are compared as keys, and `HOME` is pinned to the tree the client reads.** MSYS rewrites a POSIX path argument before native `git.exe` sees it, so the installer wrote `core.hooksPath` in one spelling and compared it byte-for-byte against another: a second run read its own wiring as a foreign owner and refused, while `verify.sh` told the operator the commit gate was missing with the gate installed. Both sides fold through one normalization now, which is a comparison key and never a stored or printed path. Separately, Git Bash resolves `HOME` from `HOMEDRIVE`+`HOMEPATH` before `USERPROFILE` while `claude.exe` uses `os.homedir()`; on a roaming profile they are different directories, and the installer had been writing `CLAUDE.md`, `settings.json` and every backup into a tree the client never opens — with `verify.sh` reading that same wrong tree and staying green. `install.ps1` pins `HOME` to `%USERPROFILE%` before delegating, and `verify.sh` compares the two for the operator running `install.sh` under Git Bash directly.
- **The Windows prerequisites are decided in PowerShell, and nothing elevates unasked.** `install.ps1` treated every prerequisite as best-effort and then delegated into `install.sh`, whose first act is a hard abort — so a clean machine spent the whole provisioning phase to die inside a bash script with the actionable warning already scrolled away. `Assert-Prerequisites` now runs before delegation and names every unmet requirement at once with the command that closes it, including a Node floor of **22** (what the fallow npm package requires) and a fallback probe for `claude` at its documented native location. `-SkipPrerequisiteCheck` waives the probes for an environment they cannot see — never Git Bash, which the hooks need at runtime and not just to install. Elevation becomes a policy both halves share: user scope first, a machine-wide retry only on the one winget exit code meaning the package ships no per-user installer, only after explicit consent, and never at all under `-Yes`. `install.sh`'s own winget calls had no scope and no guard, so they defaulted to the machine-wide install this forbids and died under `errexit` on a benign non-zero exit; bash cannot read winget's HRESULT through an 8-bit status, so it asks on any per-user failure — asymmetric, and wrong only in the direction of not elevating. `-NoGitHook` closes the flag `install.sh` always accepted and no documented Windows entry point could reach, and CI gains a PowerShell parse step plus a delegation smoke test that records argv with its boundaries marked, so a PS 5.1 splat collapsing four flags into one string goes red instead of reaching an operator as an unknown-flag death.
- **fallow installs from npm, pinned, and counts like every other MCP.** ADR-0066 made fallow the one MCP reported on a `note:` and never counted, because it built from a Rust toolchain nothing here provisions and a hard check would have made the documented one-step Windows path red by construction. That reason is gone: the npm package ships prebuilt binaries for win32-x64, win32-arm64, both macOS arches and linux gnu and musl, so `npm install --global fallow@3.14.0` provisions it on every supported host with Node 22 — which the Windows preflight now requires anyway. Cargo stays documented as an alternative rather than a prerequisite. It is provisioned everywhere, not only on Windows, which is what keeps existing Linux and macOS operators from flipping red the moment the check counts. On Windows the bin is a `.cmd` shim and npm drops an extensionless sh shim beside it that `command -v` finds first and a native client cannot spawn, so the resolver probes the `.cmd` explicitly, asking npm for its own global prefix rather than assuming `%APPDATA%\npm`. And the wiring stops short-circuiting on an entry's mere existence: `claude mcp get` exits 0 for an entry whose command cannot be spawned, so a machine wired by an earlier run to the bare name reported success while the now-mandatory check stayed red and re-running the installer could never clear it. The wired command is read back and compared now, and where it differs the operator is handed the two-step repoint rather than having their user-scope MCP state deleted for them.
- **engram is provisioned, and the summary stops reporting what it never checked.** The installer reported "engram: OK — plugin installed" from a plugin install's exit code while the binary that plugin's `.mcp.json` actually launches was never installed or even probed: a clean Windows box got a green summary and then a bare `FAIL` from `verify.sh`, the one failure in that file carrying no fix string. It is installed now from the pinned release, checksum-verified against `checksums.txt` from the same tag, per-user and without elevation, on Windows and macOS as well as Linux. Presence is never taken for health — the checksum proves which bytes arrived, and running them proves the machine will let them run, so both the placing branch and the already-resolved branch ask the binary to answer; a quarantined copy (upstream documents scanners flagging its unsigned prebuilt releases) surfaces as itself instead of as a PATH diagnosis for a machine whose PATH is fine. The probe reads the PATH Claude Code reads — the persisted machine and user scopes — not this shell's. context7's legacy user-scope entry is no longer deleted before the replacement is confirmed Connected, and impeccable's verdict now comes from the client's own plugin list rather than an install exit code.
- **`oso-state` and Git Bash are published into `settings.json` instead of hoped for on a PATH.** Claude Code injects the plugin's bin into the Bash tool PATH undocumented, and that injection already failed on Windows; when it fails the hook exits clean, so there was no signal at all and every skill fell back to a bare `oso-state` that resolved to nothing — the state layer degraded in silence while the hooks looked like they were running. An absolute `OSO_STATE_BIN` is written into the `env` block from the client's own record of where it installed the plugin, and read back before it is reported. `CLAUDE_CODE_GIT_BASH_PATH` rides along, because all five hooks are `.sh` and a native Windows client without Git Bash loses every gate; `install.ps1` already found that path and hands it over rather than writing it, since PowerShell 5.1's `ConvertTo-Json` defaults to a depth of two and would flatten the nested hook arrays `settings.json` holds. Both writes are jq, on the bash side, after the pre-image that makes them restorable. A value the operator set is never overwritten, one that no longer resolves is repaired, and a stale one on a run handed no candidate is reported rather than guessed at. The desktop app shares `settings.json` with the CLI, so one write covers both surfaces.
- **The backup `install.sh` promises now exists, and holds the state it claims.** `confirm_plan` printed a backup location in phase 1 and the directory was not created until phase 6, so any earlier failure left a path that had been promised and never made. Worse was what it held: `settings.json` is rewritten by three phases before the copy was taken — marketplace add writes `extraKnownMarketplaces`, plugin install writes `enabledPlugins` — so the snapshot held the run's own work rather than what it replaced, and a deliberate `"impeccable@impeccable": false` flipped back to `true` with nothing to restore from. The pre-image is taken in phase 2 now, beside `~/.claude.json` and the plugin registrations, and the later copies are gone so one path means one thing. The coverage report stops claiming registrations it did not hold and names the pre-release `backup-*` snapshots that sit outside this root, and retention reuses `lib/install-backup.sh`'s size bound rather than growing a second one.
- **Windows gets a double-clickable verifier, and CI a report it can read.** README presented `install.bat` as the Windows path with no terminal needed and then offered `bash bootstrap/verify.sh` from a shell as the only way to check the result. `verify.bat` is its sibling: the same Git Bash candidates in the same order, the same pause-only-when-Explorer-launched-it guard, and the same `HOME` pin — without which the new artifact would read a different tree than the client and report green on nothing. `verify.sh` also gains a check for a stored Git Bash path that no longer resolves, and a Claude Desktop line that reports the app on a `note:` either way, present or absent, saying what its presence does not prove — Desktop is an application no installer here provisions, so nothing about it can fail a run and the tally never counts it. CI pins the SET of check names beside the count, since every counted check this release adds fails in a fixture `HOME` by construction and a bare number cannot tell a regression from an absence; `windows-latest` runs `verify.sh` and `verify.bat` for the first time, at Node 22 so it exercises the floor rather than whatever the image carries. The line-ending class is closed rather than patched again: two data files were pinned by name three slices earlier and `plugin/hooks/hooks.json` was the next to reopen it, so one tree-wide `* text=auto eol=lf` replaces every name and extension pin. Nothing was renormalized, every published hash still matches, and three pre-existing Windows failures close on the way past.
- **The record catches up, and the false claims go with it.** New [docs/windows.md](docs/windows.md) is the end-to-end Windows guide — real prerequisites, both install routes and every flag, what per-user-with-consented-elevation means, fallow, Claude Desktop's Code tab versus its chat tab, how to read `passed: N, failed: M`, and troubleshooting organized by the symptom the installer or verifier actually prints. README stops saying Windows needs "nothing pre-installed" (winget is required for the provisioning path, and since this release the preflight fails closed), stops implying CI validates both plugin manifests on every runner (`claude plugin validate` runs on ubuntu alone), and no longer presents `bash bootstrap/install.sh` under Git Bash as an equivalent fallback without naming the three things it does not give you. Three decisions record the design: ADR-0127 (the Windows runtime and the state layer), ADR-0128 (the fallow contract, **superseding ADR-0066**) and ADR-0129 (provisioning and elevation), taking the log from 126 files to 129 — with ADR-0021 rewritten to describe the fail-closed preflight owner `install.ps1` has become rather than the thin delegator it was.

## 0.19.0

**Reinstall required** — Codex's installed hooks and their published trust-boundary hashes changed again, and `bootstrap/install-codex.sh`'s own transaction gained a state migration and a CLI-version precondition it did not have before. Confirm Codex already sits at `0.146.0` first (`npm install --global @openai/codex@0.146.0` if not — the installer no longer runs that upgrade itself), pull the release, run `bash bootstrap/install-codex.sh --yes`, restart Codex, and start a new thread before testing `$oso-code:plan`.

0.18.5 was cut but never published — `git tag` still reaches only `v0.18.2`, and 0.18.3 through 0.18.5 exist only as commits. Its native Plan Mode attestation work is folded into this minor rather than left standing beside it, together with the half its own commit admitted shipping without: Codex never injects `shell_environment_policy.set` into the user-hook command runner, so `${OSO_STATE_BIN:-oso-state}` resolved to nothing on `PATH` and every plan capture blocked on a sentence that named no cause. The plan hooks now fall back to the runtime sibling `$HOOK_DIR/../bin/oso-state` — the shape `publish-subagent-handoff.sh` already used — and route the previously discarded stderr into the event log.

- Codex 0.146 derives hook `permission_mode` from approval policy and therefore emits `default` during genuine native Plan turns; Oso no longer treats that field alone as collaboration-mode evidence. One exact-turn resolver instead binds the hook's `session_id`, `transcript_path` and `turn_id` to the host-generated `task_started.collaboration_mode_kind` event — rejecting foreign sessions, duplicate turn events, symlinked transcripts and unknown modes before falling back to the documented permission field — and governs plan entry, marker-bearing `Stop` capture, Plan feedback that invalidates a pending document, exact native approval after the client mode transition, and explicit cancellation. Plan capture accepts the exact marker as the final logical line when Codex `Stop` appends its own observed terminal LF, and the approval document now accounts for all five read-only planning phases plus a doubt-pass outcome.
- Every explicit delegated launch now spawns through the installed host's real `MultiAgentV2` contract: the harness had been asserting `fork_context=false`, a parameter the installed `codex-cli 0.146.0` binary rejects outright, and every one of those launches would have failed against a real host. It now sets `fork_turns="none"` and names the child through `task_name`, and `bootstrap/verify-codex.sh` gains a standing check that greps the resolved Codex binary for its own contract literals so a future host rename is caught locally instead of surviving behind assertions that only checked the harness's own prose.
- Giving feedback on a pending plan now amends it in place instead of discarding it: an ordinary Plan Mode reply appends to `current.md` under a `## Plan Mode feedback` heading and asks the model to present the delta, rather than wiping the pending state and forcing a full re-presentation for one requested change. `Implement the plan.` still binds the whole document, not only its digest — an amendment made since the plan was presented fails approval on a content comparison, so nobody can approve text they have not been shown. `CANCEL OSO PLAN` is unchanged. A second, narrower lane lets the harness itself propose a correction to a slice that has not started, on a cited file and line, authorized by one operator confirmation instead of a full return to Plan Mode.
- The state schema gained a second identity key, `plan_approval_session`, apart from the existing ownership `session`: Codex's fixed `OSO_AGENT` marker is what lets "last write wins" work as an ownership rule on a host with no per-call session id, but the same marker was also silently orphaning a pending plan's approval identity on every later model-issued write, which could leave every local tool denied for a session that had presented nothing. `bootstrap/install-codex.sh` now migrates any pre-split state file inside its own transaction: a pending plan that still names its real presenting session is backfilled with the new key, and a pending plan the ownership marker already clobbered — unrecoverable either way — is cleared instead of left standing as a repository armed forever with no session able to disarm it.
- A denied tool call, edit or commit now names what it denied — the tool, the file, or the exact command — on its own audit-log line, versioned and carrying which gate fired and on which hook event, so a denial is diagnosable from the log alone; six previously identical malformed-plan-marker messages now each name their own structural cause. Every `PreToolUse` gate hands back an executable next step naming the real binary, session, or allowed tool (or says plainly that no remedy is known) instead of a menu the operator had to translate themselves.
- `blocked` becomes a first-class verdict across every judge that can emit it, each routed to something concrete instead of a dead end; `oso-debt-sweep` and `oso-triage` move to a writable sandbox to match the project commands their own contracts already required them to run. The orchestrator now reports what it armed, launched, and closed as five short milestone facts on every host, closing the "no me dice casi nada" gap on Codex, which draws no progress card of its own.
- `bootstrap/install-codex.sh` no longer installs the Codex CLI. It refuses to run at all unless the installed binary already sits at the pinned `0.146.0`, naming `npm install --global @openai/codex@0.146.0` as the command to run first — the previous in-place upgrade ran before the install transaction began, outside anything its own rollback could ever revert. Two more integrity defects close alongside it, from an external audit: a symlinked role-file destination is removed before its replacement is written, so the installer can never write a new role's bytes through a link to an arbitrary target; and a runtime path that differs from the real one by a single unescaped regex metacharacter no longer normalizes to, and passes, the published hash it does not actually match.
- `bootstrap/restore-codex.sh` is a new, standalone operator entry point — `restore-codex.sh [--yes] [--list] [<backup-name>]` — that lists or restores any `install-backup-*` snapshot from that backup's own recorded manifest, so a backup made by an older or newer release still restores correctly. Automatic backup pruning by total size (a 300 MiB budget) stays a standing no-op until a restore has actually been exercised at least once on that machine; only `restore-codex.sh` itself can set the marker that turns pruning on, so an operator's one recovery path is never deleted before it has been proven to work.
- The Impeccable design-bar skill is now fetched at a pinned Git tag (`skill-v4.0.2`) instead of the bare, unpinned upstream repository, and the mounted skill's own version is checked against that pin after every fetch. The authenticated integrator smoke now launches Codex against a disposable `CODEX_HOME` instead of the operator's real one — the project-trust write it provokes is unconditional and un-suppressible by any flag the installed CLI exposes — closing the defect that had already left nine orphaned project tables in a real `~/.codex/config.toml`; its handoff proof now requires a spawn, a wait and a consume to agree on one Codex-assigned agent id rather than trusting a self-reported command string. `codex login status`, the smoke's own delegation, and the Engram repair's downloads are all time-bounded now, so none of the three can hang the installer indefinitely.
- The runtime allowlist table gained a capability model — a read/write/role class, and whether a name is protocol-mandated — checked locally against each locally-wired MCP server's own live tool listing, so a future missing or renamed mandated tool fails the verifier the first time it runs after the drift lands, instead of surfacing only as another live operator denial.
- The `oso` permission profile's three broad grants are narrowed regardless of when the profile applies: `.git/config` is read-only under it (closing a `core.hooksPath` redirect route), the cloud-metadata SSRF address is denied on both its IP and hostname spelling, and the secret denylist gains named per-tier env files, private-key formats and basenames, and whole credential-store directories. The profile itself stays the machine's own default (`default_permissions = "oso"`) rather than moving to a per-invocation opt-in — the installed CLI has no per-project selector to opt into, so scoping it narrower would have meant typing a flag on every single Codex launch, forever, for a margin the rest of this narrowing already covers.
- A stale-state warning at session start now names only this repository's own runtime file, never an unrelated checkout two directories over; and a wave's worktrees now cut from the commit its own previous wave actually landed rather than from the change's frozen base ref, so a wave-2+ slice depending on an earlier wave's contract runs against a tree that actually contains it.
- The record catches up: 22 new decisions (ADR-0105 through ADR-0126) take the log from 104 files to 126, `tests/plugin-lint.sh` grows from 13 rules to 22, and CI now pins `bootstrap/verify.sh` and `bootstrap/verify-codex.sh` to their exact fixture `failed:` counts (8 and 14) instead of only checking that a summary line was reached at all.

## 0.18.4

**Reinstall required** — Codex bootstrap composition and the installed shared delegation protocol changed. Pull the release and re-run `bash bootstrap/install-codex.sh --yes`; the transaction preserves Engram memories, personal Codex configuration and Oso plan artifacts.

This patch closes the two failures and one side effect exposed by the first real 0.18.3 post-install verification:

- Reinstall now normalizes Engram's two root instruction pointers after `engram setup codex` and before replacing Oso's managed TOML region. Engram 1.20.0 may move those keys inside that region when it inserts them before the first table; Oso now composes the two owners instead of deleting Engram's pointers.
- An unregistered stale Engram marketplace cache is repaired only when it is a clean, non-symlinked Git checkout with the exact official remote and Engram manifests. The cache joins the installer transaction, registered ownership is untouched, and modified or unknown state fails closed instead of being removed.
- Every explicit Codex `agent_type` launch now uses fresh context with `fork_context=false` and a complete payload. The authenticated integrator smoke demands that shape, avoiding Codex's rejection of a full-history fork combined with an explicit role.
- The authenticated smoke removes only the exact temporary `[projects.*]` table Codex adds for its disposable repository. Cleanup reads the latest config, retries if it changed, preserves unrelated project tables and multiline decoys, and never restores a whole stale snapshot.

## 0.18.3

**Reinstall required** — Codex user hooks and the installed `oso-state` runtime changed. Pull the release and re-run `bash bootstrap/install-codex.sh --yes`; existing Engram memories and approved plan snapshots under `~/.local/state/oso-code/plans/` remain outside the replacement transaction.

This patch repairs the first live Codex plan lifecycle instead of requiring the broken rail to approve itself:

- `$oso-code:plan` now fails immediately outside Codex native Plan Mode, through both the `UserPromptSubmit` hook and the skill wrapper fallback. Enter `/plan` or use Shift+Tab first; Oso never pretends it can switch the client mode itself.
- Codex's native approval prompt, `Implement the plan.`, replaces the duplicate `APPROVE OSO PLAN` interaction. The common native phrase is consumed only for a same-repository, same-session pending digest and remains ordinary conversation everywhere else.
- A Plan-Mode `Stop` persists the human document outside the repository. Native approval promotes `presented-<digest>.md` to immutable `approved-<digest>.md`; `current.md` is the operational plan and carries a revision counter.
- Explicit operator-requested hot slices may amend `current.md` and the Engram plan without another Plan-Mode cycle when they preserve frozen intent, scope and ledger decisions. Material changes are deferred to roadmap work by default or return for fresh native approval when required by the active outcome.
- Claude Code keeps its existing `ExitPlanMode` adapter and receives no Oso-owned plan artifact or approval-hook change.

## 0.18.2

No reinstall is required for an existing 0.18.1 installation. This release clears Bash 3.2's inherited command hash before asserting that the isolated SubagentStop fixture cannot resolve a bare `oso-state`; the production sibling-binary fallback and its installed bytes are unchanged. Native Bash, Bash 3.2 and Windows now judge the same fresh-`PATH` condition instead of letting Bash 3.2's cached lookup produce a false failure.

## 0.18.1

**Reinstall required** — `bootstrap/` changed. Pull the release and re-run `bash bootstrap/install-codex.sh --yes`; existing Engram memories remain in `~/.engram` and are not replaced.

This patch closes the runtime defects found during the first clean Codex installation of 0.18.0. Fallow, repository state, delegated handoffs and Engram now survive the real user-hook environment rather than only the isolated installer fixtures:

- Codex resolves `fallow-mcp` from `PATH` or the user's Cargo bin directory and writes the reachable absolute command into its managed config. The same profile grants the oso-code state root and worktree root while retaining the secret-file denials.
- The runtime allowlist recognizes Codex's normalized `collaboration*` tool names without dropping the compatibility aliases, so armed apply/verify flows can launch, message, wait on and inspect their agents while unknown tools still fail closed.
- `SubagentStop` publishes receipts under the fixed `OSO_AGENT` marker and resolves `oso-state` from the installed runtime when the hook process receives neither the binary override nor a useful `PATH`.
- `bootstrap/repair-engram-codex.sh` backs up SQLite and a portable JSON export, verifies and installs Engram 1.20.0, rewires Codex, normalizes Engram's root pointers outside oso-code's managed TOML region, validates the candidate config through Codex, and finishes on the authenticated 16-check verifier. Its fixture rail rejects malformed ownership and physical symlink escapes before any operator path can be changed.
- Regression coverage grows to 976 passing cases, including real installed-layout handoffs, Cargo-home fallow discovery, normalized collaboration operations, Engram setup ordering, idempotent repair and fail-closed filesystem containment.

## 0.18.0

**Reinstall required** — `bootstrap/` changed, and neither host's plugin payload carries all of its installed runtime: pull the repo and re-run `bash bootstrap/install.sh` for Claude Code or `bash bootstrap/install-codex.sh` for Codex.

oso-code now ships one harness across Claude Code and Codex. The behavioral contract lives in shared mode bodies; thin platform adapters bind each host's invocation, questions, approval, tools, state and security-review route without pretending those APIs are identical. The Codex port is pinned to the fully probed `0.146.0` CLI rather than `@latest`, and the parity ledger is now a release artifact: every intentional loss or narrower guarantee is named instead of hidden behind a claim of equivalence.

- Plan, quick and debug gain Codex wrappers over the same neutral flows, with Codex-specific routes for `request_user_input` (at most three questions per round), plan approval, delegation, Impeccable and native `codex review`. Seven TOML roles carry the apply, verify, integrate and judge contracts under explicit model, reasoning and sandbox choices. Two roles deliberately need outer `danger-full-access`: the merge-only integrator must reach git metadata and external worktrees for merge/cleanup, and the security judge must launch its nested authenticated `codex review`; their semantic contracts remain narrow, while applier and verifier stay `workspace-write` and the other judges stay read-only.
- Codex's local approval rail binds the exact presented plan through `Stop`, consumes its literal token through `UserPromptSubmit`, and keeps ordinary tools closed through `PreToolUse` until approval. It is a real local technical gate, but a narrower one than Claude's native approval UI: hosted and specialized surfaces do not inherit the complete rail, and `write_stdin` against an already-running process never crosses `PreToolUse` at all.
- Runtime state is keyed by the SHA-256 of `git rev-parse --path-format=absolute --git-common-dir`, so a main checkout and its linked worktrees reach one gate. The state deliberately survives a killed session and is shared by two sessions in the same repository; that makes the rail fail closed across worktrees, but also means concurrent sessions can overwrite each other's state.
- `bootstrap/install-codex.sh` is transactional and idempotent. It pins Codex, verifies every installed hook dependency against release-published hashes, stages the skills-only Codex marketplace plugin, renders the user hook manifest, installs seven agent roles, owns a primary `config.toml` region plus a leaf-level block for `features.hooks` and `features.multi_agent`, wires context7/fallow/Engram, mounts Impeccable, and rolls every replaced artifact back on failure. The `[features]` table remains shared, so Codex preferences such as `prevent_idle_sleep` survive installation and reinstall. An exact earlier `core.hooksPath=<checkout>/plugin/git-hooks` migrates to the self-contained runtime only when no sibling entry would be lost; copied or mixed hook directories remain foreign. Codex plugins cannot bundle user hooks or agent TOMLs, so those are installer-owned artifacts and hook trust remains an explicit `/hooks` step.
- `bootstrap/purge-codex.sh` is a separate, optional migration boundary rather than an installer side effect. It backs up complete `~/.codex` and `~/.agents` roots under mode `0700`, verifies its hash manifest before removing either source, and restores without following root symlinks or overwriting an existing destination. It never installs Codex or runs `codex login`; those remain explicit operator actions.
- `bootstrap/verify-codex.sh` reports every local component and always reaches `passed: N, failed: M`: pinned CLI, plugin payload, installed hooks and hashes, all roles, managed config, MCPs, Impeccable, git gate, state round-trip and an authenticated integrator/delegation smoke. Because Codex propagates a live parent sandbox override to spawned agents, the disposable smoke parent uses the integrator's `danger-full-access` instead of narrowing the child back to `workspace-write`; success still requires an observed delegated spawn, merge, worktree removal and branch deletion inside the temporary fixture. CI syntax-checks every new shell and proves the verifier footer against a failing fixture, but it deliberately performs no login and no real `codex exec`; the authenticated smoke stays on the operator's machine.
- The design record adds the Codex baseline, repository-keyed state, host-adapter/distribution boundary and the parity-ledger contract. The blueprint and `docs/parity-codex.md` name the remaining differences: Codex uses compact global `AGENTS.md` instructions that the host may compact, `codex review` rather than Anthropic's native reviewer, user-level hooks with explicit trust, shared state between sessions in one repo, and a real smoke test that cannot be made truthful in unauthenticated CI.
- Release checks now exercise the documentation contracts behind linter rules 10–12: every ADR records where it landed, every skill citation resolves bidirectionally through `Implemented-in:`, and README plus the linter header keep the declared thirteen-rule count honest. The Claude and Codex plugin manifests advance together to `0.18.0`; neither marketplace catalog invents a duplicate version field.

## 0.17.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Execution stops being one slice at a time — a `/plan` change now chooses, on a number the slicing phase computes, between the sequential loop it has always run and a parallel one that puts each slice of a wave in its own worktree, merges the wave through an agent that judges nothing, and re-verifies the tree they add up to before the next wave arms. The slicing bar is untouched on purpose: slices are still cut vertically and the graph derived from the cut, never the other way round. The commit boundary moves with it: a commit is part of the flow in both modes, and a push and a PR are what the operator is still asked for. The hook suite grows from 338 to 380 cases, the linter from eleven rules to twelve, and the decision record from 75 numbered files to 93:

- `/plan` §4 keeps cutting the same vertical slices and then says how they run. Each slice gains a `Depends-on` field filled from a dependency graph read off the surface map rather than guessed at slicing time, and that graph carries four edge sources — a contract and its consumers, shared state, a data flow, and VERIFICATION-BAR COUPLING, the one none of the other three shows, where two slices sharing no contract, no state, no data flow and no file still cannot pass the project's bar apart (this repo is its own example: the linter binds a cited decision id to the `Implemented-in:` line that names the citing file back, so the slice writing the citation and the slice writing the decision are one edge apart however disjoint their files read). Files overlap drops to a secondary physical check, because the Files field is declared rather than measured. The graph groups into WAVES with two structural boundaries — wave 0 is the design-foundation slice alone in the main checkout, and a CONTRACT slice never shares a wave with a MIGRATE one, whose pre-delete completeness check would otherwise grep a tree its siblings' migrations have not reached — and the phase closes on one question carrying the widest wave's width, the arithmetic (a wave costs its slowest slice plus one full integration gate; §1–§5 and §7 do not parallelize at all) and a recommendation: parallel at a width of 3 or more, sequential at 2, a concurrency cap of 4 by default, and no question at all when the ledger's base ref is `none`. It sits before `ExitPlanMode` so the mode rides inside the plan the operator is already approving instead of adding a second gate.
- §6 gains the wave loop beside the sequential steps it leaves untouched. A wave arms one session-global `active_slice=wave-<n>` plus a new `repo_path` key, cuts one worktree per slice at `~/.local/state/oso-code/worktrees/<sanitized session>/<slice>` — outside every project repo, on a branch `oso/<change>/<slice>` — launches N appliers in one message and N verifiers after them, and commits each slice as it goes green. That commit needs a GREEN WINDOW, and the cost is stated rather than buried: both layers of the commit rail read one session-global `verify_green` and neither can see which worktree a commit came from (the git hook because `core.hooksPath` is an absolute path every linked worktree inherits, the matcher because it reads the command line), so the orchestrator opens the flag, commits that one slice, and closes it — three commands with nothing between them, and while they run the rail is open session-wide for any commit from any tree. No hook changed. A wave integrates only when EVERY slice is green, and failure routes by class with none borrowing another's route: a red slice loops in its own worktree while its siblings run on, a merge conflict stops the integrator and reaches the operator with the conflicted tree exactly as git left it, and a red integration returns the WHOLE WAVE as a unit — a regression check names the slice whose behaviour broke, never the slice that broke it — with the fix entering as a new slice through the normal gate. The first wave also answers what §3 could only record as a question, whether this project's bar survives being run N times at once, and a wave that degrades draws an offer to finish the rest sequentially: an offer, never a gate.
- New `oso-integrator` agent (Read and Bash only): it merges one wave's branches one at a time in the order it was handed, names every repository with `git -C`, and NEVER resolves a conflict — no `--abort`, no reset, no `-X ours` — because which side wins is a decision about the change and decisions are the operator's. It judges nothing and gets no rubric: the merged tree's bar belongs to the gate after it, an `oso-verifier` run in a SECOND verdict shape whose criteria are the project's full bar plus one `failing-check` line per slice of the wave, none omitted, each reported as `holds`, `broken-by-the-merge` or `exception-declared`. That shape deliberately carries no `failing-check quality` line: whether a check was tautological or implementation-coupled was settled at the slice's own gate, and a merge cannot turn an independent behavioral check into a tautological one. Both delegation payloads now carry a WORKTREE PATH and a BASE REF in either mode, which also gives "the diff" the verifier has judged since 0.12.0 its first definition — `git -C <worktree path> diff <base ref>`, committed slice work and uncommitted alike — and the applier skips its own self-check under parallel (`skipped: parallel`), since N project bars on one machine contend on ports, test databases, build caches and lockfiles that separate worktrees do not isolate.
- New forked `triage` skill (`context: fork`, `background: false`, `model: opus`), the fourth judge to pin opus: it answers ONE question from read-only git evidence — is this red check attributable to the change under execution, or does it predate the base ref — and ends on `Triage: attributable`, `Triage: pre-existing`, or `Triage: skipped — attribution not established`, which is never read as either answer. The wave loop invokes it before it routes a red nobody can place, because the orchestrator wrote the plan the wave is executing and is the worst available judge of whether the wave is to blame. It stops at attribution and hands back evidence rather than a diagnosis: `oso-code:debug` owns reproduce-first triage, and running that flow here would put one flow in two files. It is model-invocable, since the orchestrator reaches it through the Skill tool mid-wave, so it is a skill and not a fourth mode — `/debug` keeps `disable-model-invocation: true` and the linter rule that routes every operator-only mode is untouched — and its scope guard is prose only, recorded as such.
- Worktrees are torn down where they were made and where they were abandoned. The integrator removes them at integration, worktrees BEFORE branches — git refuses to delete a branch a standing worktree has checked out and no force overrides that — the close clears whatever a wave that never integrated left standing, and the SessionEnd hook removes the survivors of a session that never got there, plus the ones an abandoned session left behind on the existing 7-day orphan pass. Always `git worktree remove` then `git worktree prune`, never `rm -rf`: a directory deleted behind git's back stays registered and the next `git worktree add` for that slice fails on a name only git still believes in. The repo to prune in comes from the new `repo_path` state key, read through `oso-state` rather than re-parsed, and every failure — an unreachable reader, a removal git refused, a prune that could not run — is logged instead of swallowed while the state cleanup goes on regardless. `oso-state` gains an `event` verb for exactly that: a worktree creation, a merge conflict and a red integration are neither tool calls nor state writes, so nothing in the harness could record them before. No teardown forces a removal, at SessionEnd as at the close: what git refuses to remove is uncommitted work in a wave that never integrated, so the tree is left standing with that work in it and the refusal is logged. The teardown alone carries 23 of the suite's new cases, 18 of them behind the single skip line the `bash:3.2` container prints for the block where git is absent.
- The commit boundary moves, and every surface that stated the old one now states the new one. A COMMIT is part of the flow and is never asked for: §6 lands one per slice in BOTH modes — the wave path already did, and the sequential path now commits in the main checkout inside the green its own step 4 writes, which the next slice's step 1 takes back — and the close commits what the close itself landed. PUSH and PR are the two that still require the operator to ask, in `/plan` §7 step 8 and in the always-loaded `bootstrap/claude-global.md` that used to read "never commit, push, or open a PR unless asked". Per-slice commits are ON by default from this version; a project turns them off in the ledger's §3 Verification row — the row that already settles the base ref and the concurrency question, and no new store — and off there leaves sequential the only mode, since a wave is integrated from the commits its slices land. What that shift costs the close is stated rather than buried: with the change already in commits, the pending working tree holds only what the close itself landed, so the base ref `/plan` passes to `oso-code:security-pass` is what gives that review the whole change instead of a fraction of it — and a base ref of `none`, where no slice committed, is exactly the case where the pending tree IS the change. `/quick` and `/debug` have no slices and land no commit of their own; what changed for them is the boundary they state.
- The record catches up with the mechanism. 18 new decisions (ADR-0076 through ADR-0093) take the log from 75 files to 93, each naming what it amends rather than quietly restating it — ADR-0001's never-writes invariant is why the integrator exists and is still narrowed exactly once, ADR-0051's two layers are why the green window has to exist, ADR-0037's diff finally has a definition, ADR-0063's four assignment kinds are unchanged with two coordinates added to one of them — and `docs/blueprint.md` is edited where this change made it false: phases 4, 5, 6 and 7, the two Hooks bullets that describe the armed flag and the per-session state file, and the index. `tests/plugin-lint.sh` grows a twelfth rule holding the launch site of the new agent to naming the wave's worktrees, base ref and branch list, the three things the orchestrator decides and writes down nowhere else; the hook suite goes from 338 to 380 cases and from 322 to 346 under `bash:3.2`, where five skip lines now stand for the 34 that need git or jq.

## 0.16.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

The third mode made whole, and the release rails under it — `/debug` shipped in 0.13.0 unreachable from the always-loaded file that routes a session into a mode, with a close whose green one of its own branches traded away; the installer read a hard `cargo` failure as success and could repoint the marketplace at a working tree nobody chose; the verifier died mid-report on any input it could not read; and the design record was a 13-entry prose log where a decision could only be found by reading all of it. The hook suite that judges the hooks grows from 313 to 338 cases, the linter that judges the prose from six rules to eleven, and the log becomes 75 numbered decisions held from both ends:

- `/debug` is reachable and its contract closes end to end: `bootstrap/claude-global.md` routes "something broke" to `/oso-code:debug`, which is the only thing that can send a session there — the model never invokes the mode on its own — and a linter rule now holds that routing for every operator-only mode. §3's named regression test gains a bounded escape (`Verify-exception: <reason>`, only where the fix touches no code the suite can execute — a Dockerfile, a CI workflow, an editor config — and never for the §1 hypothesis override, whose test is precisely what tells whether the hypothesis was true), and §4 step 2 hands `oso-verifier` all four things it reads: the fix criteria, the zero-warnings commands the diagnosis froze, the rubric path, and the frozen diagnosis itself as fix-decision context — under that name and never "as ledger", since this flow freezes none, which the agent's own contract now states where it used to assume a ledger and fail a new abstraction for the absence of one nobody handed it. `quality-pass` names its real callers, quick and debug rather than quick and plan, and reads the diagnosis's frozen commands in debug mode. And the close's debt-sweep offer is ADDITIVE to the quality pass instead of replacing it — the old wording traded away the `Quality Pass: passed` that step 4's green reads, so that branch could never go green at all — with a fix route (findings → `oso-applier` as a debt cleanup, re-judged until `Debt Sweep: clean`), a re-run of the pass over whatever cleanup landed, and `Conformance: skipped — no ledger provided` named as this flow's expected answer rather than the re-invocation trigger the same token is in `/plan`.
- The hook suite grows from 313 to 338 cases while its hand-rolled assertion blocks collapse from 37 to 8 behind five shared helpers — a consolidation that closed a hole rather than a style pass: a case that ran a hook bare and then read what it left behind reported `ok` even when the hook crashed after doing the work, because nothing judged the run itself. 322 of the cases run in the `bash:3.2` container, where four skip lines stand for the 16 that need git or jq, and 335 run with jq hidden from PATH.
- `bootstrap/verify.sh` always reaches its summary: seven abort sites now read their inputs in a form that cannot die under `set -euo pipefail`, keep the failing command's own stderr inside the value they report, and fold it to one line so a reason never orphans below the verdict it belongs to — a vanished input goes red with its reason instead of green on nothing scanned, and an operator can tell a broken install from a broken verifier. The optional fallow MCP becomes a `note:` that moves neither tally, since it needs Rust that no prerequisite row requires and `install.ps1` provisions none, so a hard check made the documented one-step Windows path red by construction. The `--no-impeccable` opt-out is recorded as a marker file the installer writes on that path and CLEARS on every install that wires the plugin, which is what makes `failed: 0` reachable for an operator who declined the design bar without letting a stale marker report a genuinely failed install as their choice. The npx probe gets a portable in-shell 20-second bound — `timeout(1)` is GNU coreutils and macOS ships none — whose kill reaches the node children npx spawns instead of orphaning them, and `OSO_VERIFY_SKIP_SLOW=1` drops the two checks that re-run the suite or fetch from npm. That flag is what lets CI run the script at all: a new step is the first thing anywhere that has ever executed `verify.sh`, and it gates on the `passed:` line rather than the exit code, because against a fixture HOME the install checks fail by design while only a dead substitution removes the summary.
- The installer stops scoring failures as successes and stops repointing the marketplace behind the operator. The `*already*` substring arm is gone from both `run_or_fail` and `run_wiring`: measured against the clients, `claude plugin install` and `cargo install` both exit 0 on an already-installed package, while `cargo install` exits 101 when an untracked binary already occupies the destination and says "already" in the message it fails with — so the arm only ever turned a hard failure green, and the one command that really does report an existing entry as a failure, `claude mcp add`, gets an explicit `claude mcp get` re-check instead. Marketplace registration moves into `ensure_marketplace_source`: a source already registered as a local `directory` is named to the operator with what it costs (`marketplace update` refreshes nothing there, and `plugin update` installs whatever that tree currently holds) and is repointed at GitHub only when the remote is reachable AND the operator consents — this plugin is developed from a clone like that, where an unasked repoint swaps unreleased edits for the published release. A failed `marketplace add` is classified off the client's own message (`seed-managed`, `policy-blocked`, `invalid-source`, `invalid-manifest`, `unreachable`, anything else `unknown`), only `unreachable` earns the local fallback, and that fallback refuses a `$REPO_ROOT` carrying no `.claude-plugin/marketplace.json` — piped through `curl | bash` there is no `$BASH_SOURCE`, so the path lands on `/` under bash 3.2 or on the parent of the operator's cwd, and the manifest is the one thing that tells a real clone from either. The run moves into `main()` behind a `BASH_SOURCE`/`$0` guard so the suite can source the file and exercise those decisions directly, and the wiring summary survives an empty array under the bash 3.2 `set -u` macOS ships.
- `install.ps1`'s `Update-EnvPath` unions instead of replacing: the registry `Machine`+`User` scopes come first, because appending them behind a stale process PATH re-shadows the very tool the refresh exists to expose, and every entry that lives only in this process survives — an nvm/fnm/volta shim, a caller's prepend, a CI-injected path, a portable Git is in no registry scope at all, and dropping it took `Find-GitBash`'s own `git` with it.
- README states the two tiers an update travels, because the harness reaches a machine by two routes: `claude plugin update oso-code@oso-code` carries everything under `./plugin`, which is the marketplace entry's whole source and works with no working copy of the repo at all, while a release marked **Reinstall required** means `bootstrap/` changed and only re-running `install.sh` carries it — that marker is now on every past release whose `bootstrap/` moved, back to 0.1.0. It also says how to read `verify.sh`'s report: `ok:`/`FAIL:` lines, a closing `passed: N, failed: M` that is also the exit status, and `note:` lines that are not checks and move neither number. Both manifests are completed — `plugin.json` gains `homepage`, `repository`, `license` and `keywords`, and `.claude-plugin/marketplace.json` gains its `$schema` and the entry's `author`, `category` and `keywords` — so `claude plugin validate --strict .` has as much to judge as `--strict plugin`.
- The blueprint's 13-entry amendment log becomes 75 numbered decisions under `docs/decisions/`, one per decision, ids fixed at creation because the skills cite them, with the blueprint keeping an index sorted by DATE — a 2026-07-06 entry sat ten days out of order in the log, a filing error the ids preserve and the index corrects — and every file carrying a `Reconciled:` line that says where the decision landed in exactly one of four words (`applied`, `superseded`, `elsewhere`, `nowhere`) plus the text naming the location, since a marker naming no location answers nothing. Two properties of the old log are carried forward as facts rather than corrected: a contradiction between two entries over `tests/plugin-lint.sh`'s rule count, and the provenance of three entries edited in place, which `git log -S` recovers where `git blame` reports only the annotating commit. `tests/plugin-lint.sh` grows from six rules to eleven to hold the record: the always-loaded routing file names every operator-only mode; every line that launches `oso-verifier` names the payload it hands it; every decision says where it landed; every decision a skill cites resolves to one of those files AND is named back by it in `Implemented-in:`, so a citation retargeted to the wrong decision breaks the second half even when the first still resolves (the twelve references in plugin prose are held from both ends); and the prose that counts these rules — the linter's own header and README's linter row — names the number the functions declare, so a count that decorates instead of describing fails; while the call-site rule that was already there keeps its at-least-one-token floor and gains a second half over it — for every axis its emitter can report as `skipped`, a site that names any of that axis's OTHER verdicts must carry the skipped one verbatim too, since a gate that reads an axis and never carries the answer `it did not run` reads that answer as neither verdict and opens its green write over an axis that never ran.

## 0.15.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Runtime enforcement that actually enforces, and the mechanisms 0.14.0 introduced made to match their own docs — a four-lens audit found both hooks failing open on a telemetry write, ~24 matcher bypasses beside 9 false denials, and a one-shot edit gate; the commit rail is now two layers over a lexer, armed sessions fail closed, every abnormal branch leaves a trace, and six contracts that described behavior the code did not have are now true:

- Two-layer commit boundary: a git `pre-commit` hook (`plugin/git-hooks/pre-commit`) wired through `core.hooksPath` is the primary gate — it sits at the commit's own boundary, so aliases (`ci = commit`), wrappers (`flock`, `ssh`, `docker exec`), an absolute `/usr/bin/git`, and non-Bash paths all reach it with zero string parsing and zero false positives — and the `PreToolUse` Bash matcher becomes the second layer for what a git hook cannot see: `--no-verify`, `commit-tree`, `update-ref`. The installer never overwrites or chains another tool's hooks: a foreign `core.hooksPath` or any non-`.sample` hook in the repo's hooks directory means the layer is not wired there, named in the wiring summary, and `--no-git-hook` opts out.
- Hooks fail closed once a session is armed and stay invisible when it is not: an absent state file allows silently and forever, while an armed session whose state file cannot be read denies and logs. A failed telemetry write no longer decides the verdict — the deny JSON goes out before logging, and logging can never retract it. Payload reading goes hybrid: jq where present, the pure-bash reader as fallback, and a `jq-absent` event so the degradation is measurable; a missing jq is never fail-closed, because a marketplace install never runs `install.sh` and a GUI-launched macOS client has no Homebrew path.
- The commit matcher becomes a lexer that separates code from text: quote, escape, and subshell state, heredoc excision with the shell-interpreter asymmetry, comment stripping, herestrings read as payloads, command-position enumeration, arity-agnostic prefix stripping (`env X=1 sudo -n timeout 60 git commit` runs git), redirect targets dropped instead of walked into the command's words (`>/dev/null git commit` is a call to git, not to `/dev/null`) and an expansion anywhere in the command word — `g${x}it`, a backtick — read as unresolved rather than literal, a basename match on the command word (so `/usr/bin/git` and `git.exe` count), and an interpreter payload resolved by position — the first non-option word after `-c`, never the line's last argument. Gated verbs are `commit`, `commit-tree`, `update-ref`, `filter-branch`, `replace`, and `fast-import`; the other history-writing verbs (`revert`, `merge`, `rebase`, `cherry-pick`, `am`, `stash`, `push`) are pinned to an explicit allow, because for five of those seven — `revert`, `merge`, `rebase`, `cherry-pick`, `am` — the bare verb and its `--abort`/`--continue` recovery form are the same token, and denying `--continue` deadlocks a session that cannot run verify mid-conflict. There is no `ask` rung — a subagent cannot answer a prompt and a headless run has no operator — so the undecidable residue allows and emits `residue-allowed`. Input and decoder bounds sit at 3072 bytes — the worst shapes measured run 126-140 ms at that bound and over 400 ms at 8 KiB without one, where what a session really sends runs in 14-25 ms — at a stated coverage cost: the commit rail stops applying to a Bash line whose command field exceeds 3072 escaped bytes.
- State discipline: every state write carries the full triple with an explicit `active_slice=none` sentinel the edit gate treats as disarmed, arming reads the state back before delegating, and abandoning a mode mid-flow clears the state. The edit gate's state-directory exemption is gone — zero callers, and its unnormalized prefix glob was a universal bypass by `../..` — and the gate now also covers `MultiEdit` plus a named allowlist of MCP writer tools (`mcp__fallow__fix_apply`), a list rather than a pattern so read-only MCP calls stay ungated.
- Audit trail: abnormal branches get their own event types (`state-unreadable`, `payload-unparseable`, `jq-absent`, `residue-allowed`), logged values are escaped, the ERR trap is scoped to the armed region, an unwritable log falls back to stderr, and every line records the client handle a native install carries in its own path — empty where the client sets none — so the next platform drift is detectable. At SessionEnd the log rotates by atomic rename at 30 days, and `*.state` files orphaned by sessions that never reached SessionEnd are pruned at 7 days under own-file and lock guards.
- The bar: the hook suite grows from 36 to 313 cases, with rc and stderr assertions inside the helpers (a crashing hook used to abort the run silently) and payload builders whose envelope shape is copied from recorded `PreToolUse` entries. New `tests/plugin-lint.sh` covers the six rules `claude plugin validate --strict` has no opinion on: the validator does parse `hooks.json`, skill frontmatter and the agents, but it never asks whether a `context: fork` skill declares `background`, whether that same skill declares an `end with exactly one of:` verdict block, whether every `oso-code:<name>` reference resolves, whether each call site of a skill that declares such a block carries at least one of its tokens verbatim, whether `security-pass` acquires its diff from a remote-qualified ref, or whether the Impeccable detect gate carries a placeholder where its pin belongs — so the linter is what holds those. `.gitattributes` pins line endings, since a CRLF checkout kills a shipped hook mid-verdict and denies every tool call that hook gates, and `ci.yml` gains a `bash:3.2` container step — the oldest bash the harness supports, and how this change verified its most delicate constraint. The three forked skills pin `background: false` and `/plan` and `/debug` name `run_in_background: false` at the applier/verifier launches; `model: opus` is dropped from `/plan` and `/debug`, where the override lasts only the invoking turn, and kept on the forks, where one invocation is one agent run.
- The Impeccable detect pin is resolved at RUNTIME from the npm channel, never off the installed plugin's version — the two ship on independent release lines, and the old instruction read the pin off the plugin's, leaving a placeholder that was never a runnable command. `npx impeccable --version` returns the numeral, the detector then runs as `npx impeccable@<numeral> detect`, and BOTH numerals are recorded — the CLI's and the plugin's, from `claude plugin list` — because the arm that judges design (the `audit` skill and its `reference/` playbooks) ships with the plugin while the detector ships on npm. Resolution happens where the network is reachable: `/plan` at its first front-touching slice, never during read-only planning; `/quick` when the front work starts; `/debug` at the diagnosis freeze. The recipe is written once, in `plugin/skills/_shared/front-surface.md`, and a pin that cannot be resolved at all — no Node, offline, registry error — is named in the mode's own record instead of left standing.
- The design-audit loop exits on an explicit TRANSLATION of what Impeccable emits: it publishes no `clean` token, so the bar reads two fields of its report — the integrity verdict is `Pass` and no P0 or P1 finding is open — and names those two fields as what moves when upstream changes shape. The operator escape, an explicitly accepted residual, stands beside that bar and never in its place; a P2 or P3 still open when the loop ends is named in the record of the mode that ran it, never dropped because the loop was allowed to end. Findings route to `oso-applier` as judge findings, and each round is proved by two things: the re-audit, and the project's own zero-warnings bar, which the orchestrator runs because an `audit` judges design and runs no project checks.
- `security-pass` acquires its own review surface on the fallback path — the native path leaves acquisition to the native skill — and that surface is the pending working tree: `git diff HEAD`, plus the full CONTENTS of every untracked file (`git ls-files --others --exclude-standard`, then read each, since a brand-new auth file is the commonest shape of an auth change and no form of the diff can see it), never `git add -N` or any other write to the index, and a base-ref range only when one arrived in its arguments. Nothing assumes a remote named `origin`: a diff against a ref that resolves nowhere dies with `fatal: ambiguous argument` (rc 128) at the one gate that must not improvise. The verdict is its own line after the body, `Security Pass: clean` or `Security Pass: findings`, separate from the `Security Pass: native` / `Security Pass: fallback` header that names only which path ran.
- `/plan`'s close reads BOTH of debt-sweep's verdicts by name — `Debt Sweep: clean` or `findings` on one axis, `Conformance: clean`, `findings`, or `skipped — no ledger provided` on the other — and a skipped conformance axis is never a pass: it says the ledger never reached the invocation's arguments, so it blocks the verify_green write. Conformance findings go to operator triage one at a time, each presented with its two readings for the operator to pick between: the CODE diverged from the decision, and the fix goes to `oso-applier` as judge findings; or the DECISION changed during the work, and the ledger is amended by a dated entry appended beside the frozen one, never an edit of it, because the PR body carries that ledger as the reviewer's evidence. `Unimplemented` never takes that route — a decision with no trace in the diff is a slice's worth of work missing, so it returns to §6 as its own slice with its own failing check, since the close path has no slice activation, no `oso-verifier`, and no failing-check gate.
- The contracts under all of it: `oso-applier` enumerates a CLOSED list of four assignment kinds — a plan slice, a debt cleanup, judge findings, a diagnosis packaged as a ledger — each carrying its own permission to change behavior, so judge findings MAY change behavior inside the scope of the finding they resolve (a design finding is a change to rendered output) where a debt cleanup may not, and that payload is self-contained: a missing ledger is never itself a reason to report blocked. `plugin/skills/_shared/front-surface.md` claims single-source status only over what it actually holds — the trigger, the pin recipe, the audit exit bar, the absence policy — while the wiring stays each mode's own, indexed by a per-mode matrix whose every cell is a pointer into the mode's section rather than a restatement of it. And `tests/plugin-lint.sh` grows from the two rules it landed with to six.

## 0.14.0

A security review that no longer leaks into the orchestrator, a design bar on every front change, and a second selective import — the harness now reviews, designs, and conforms to its own ledger. It was cut in the manifest and never published on its own: this work first reaches an install inside 0.15.0, which is what made the mechanisms below behave the way they are described:

- New forked `security-pass` skill (context: fork, agent: general-purpose, model: opus): it invokes the native `security-review` INSIDE its own fork, so Anthropic's review prompt injects there and never into the orchestrator's context, and falls back to a condensed native-derived review (>80% exploitability confidence, the native exclusions, markdown findings) when that skill is not listed in the fork — the report opens with the path that ran, and the review never runs inline. All three modes rewire their pre-commit offer to it: on acceptance invoke `oso-code:security-pass`, relay the report verbatim, route accepted fixes through `oso-applier`, and re-run until it returns `Security Pass: clean` or the operator accepts the residual.
- Front-surface design bar powered by the Impeccable plugin, with the trigger and integration contract defined once in `plugin/skills/_shared/front-surface.md`. `/plan` wires it at §2 (design bar derived from front surfaces, plus an absence policy that names the gap, gives the two-step install, and records it visibly), §3 (the pinned design detector among the Verification commands), §4 (a design-foundation first slice where the orchestrator itself runs Impeccable's `init` or `document` — a narrow ledgered exception to the never-writes invariant), §6 (design docs and the paths to Impeccable's `SKILL.md` and `reference/` playbooks in the applier payload, pinned detect in the slice verify bar), and §7 (an Impeccable `audit` → fix → re-audit loop). `/quick` runs the inline equivalents; `/debug` gets the payload and the pinned detect only when the fix touches front surface — no init/document, no audit loop, and the regression test stays the exit criterion.
- Bootstrap installs Impeccable by default (`wire_impeccable()`, the `wire_engram` pattern) with `--no-impeccable` to opt out, forwarded from `install.ps1` as `-NoImpeccable`. `verify.sh` gains two checks — the plugin listed AND `npx impeccable --version` exiting 0, the runtime path the pinned detect gate takes. The plugin check goes red when opted out; the CLI check stays green regardless, because npx resolves impeccable from the public registry rather than from the plugin install.
- Pocock adoptions, from an 18-skill gap analysis of mattpocock/skills held to the same selectivity bar as the addyosmani pass: `oso-verifier` now judges the failing check's QUALITY — tautological assertions (expected value derived from the code under test) and implementation coupling (pinning internals instead of observable behavior) are treated exactly as a missing failing check, while the rubric stays clean-code-only; `debt-sweep` gains a second axis, ledger conformance, in its own report section with its own verdict (unimplemented / contradicts-decision / scope-creep / partial), fed the bare decisions and scope but never the rationale, skipped honestly when no ledger is passed; and `/plan` §4 gains an expand-contract slicing template (EXPAND → MIGRATE → CONTRACT) whose CONTRACT slice must verify a named pre-delete completeness check.

## 0.13.0

A third mode for when something broke, plus additive safety offers on the two existing flows — the harness now debugs reproduce-first and reaches for a security review before the diff is gone:

- New `/debug` mode: a reproduce-first triage (reproduce → localize → reduce → root-cause fix → regression guard) that stops when there is no reproduction — reporting ranked hypotheses, with an operator "fix on hypothesis" override recorded in the diagnosis — then freezes the diagnosis to `oso/{bug}/diagnosis` (no `oso/index` row) and delegates the fix through the apply/verify loop. The `oso-verifier` judges the named regression test through the failing-check contract: it must fail without the fix and pass with it. Edits run under `mode=debug` (unrestricted — the edit hook fires only on `mode=plan`) while the commit gate stays mode-agnostic, pinned by two new `tests/hooks-test.sh` assertions with the hooks untouched.
- Additive offers on `/plan` and `/quick` (zero existing flow lines changed): a bug detour at intent that hands a misfiled break to `/debug`, a stop-the-line that names unrelated breakage mid-flow instead of fixing it in passing, and a pre-commit security-review offer that invokes the native `security-review` skill through the Skill tool when it is listed (else suggests typing `/security-review`) — pre-commit because the native review reads the pending working-tree diff, which dies at commit.
- Docs and manifests name the third mode: the blueprint gains a dated amendment and a Mode 3 section, and the README, plugin manifest, and marketplace descriptions all list `/debug` alongside `/plan` and `/quick`.

## 0.12.0

Anti-rationalization trap tables, an adversarial doubt pass, and a slice regression gate — a gap analysis of addyosmani/agent-skills against the harness, importing only the mechanisms the evidence supported at the gates the inventory found weak:

- Anti-rationalization trap tables (`| Trap | Reality |`, each excuse quoted verbatim beside its reality) land at the four under-protected gates: `/quick` §2 substantiality and §4 zero-warnings, `quality-pass` §3 re-verify, and `debt-sweep` §1 fallow skip. The plan skill, agents, and rubric stay untouched — already defended, and a context budget to keep.
- New forked `doubt-pass` skill (context: fork, agent: general-purpose, model: opus): a pre-freeze adversarial review of the bare decisions — intent, surface map, and decisions only, never the rationale or rejected alternatives (structural anti-anchoring). `/plan` §3 offers and recommends it only when a derived category came from a migrations, security, or rollback surface; the orchestrator reconciles its findings against the stored rationale (noise vs actionable, actionable resolved like §6 blocked questions) over a single pass with operator re-run, a hard cap of 3 cycles, and a doubt-theater signal.
- Slice regression gate: `/plan` §4's Verify bullet now requires at least one automated check that fails without the slice — new or extended by the slice, exercising its behavior — or a declared `Verify-exception: <reason>` visible in the approval document. `oso-verifier` judges it by reading the diff, under an explicit no-time-travel prohibition.

## 0.11.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Repaso de cambios, derived decision categories, and an anti-swallow delivery contract — the harness fixes a TUI bug that was silently hiding its own explanations and redesigns the model that had been built on top of the gap:

- Repaso de cambios (change recap) replaces the didactic walkthrough: a fixed three-section brief (Qué se va a realizar / Decisiones del ledger que lo moldean / Cómo va a funcionar), ~20-line soft cap, written in the operator's language at their depth preference — never a forced didactic register — that heads the plan argument the native `ExitPlanMode` gate renders, immediately followed by the full plan detail. No confirmation loop; `ExitPlanMode` is the single approval gate.
- Decision-rounds category model redesigned: an invariant core of five categories (Contracts, Architecture, Errors, Verification, Reuse) plus categories derived per change straight from surface evidence, each citing the surface that motivates it — the core is the fallback question generator only when exploration surfaces nothing, superseding the fixed category table's audit-floor role. Question rounds cap at 4 (the `AskUserQuestion` platform ceiling), down from 3–5.
- Anti-swallow delivery contract: the Claude Code TUI drops assistant text that precedes a tool call in the same turn, so operator-facing content now always ends its turn as plain text before any later-turn tool call, and round context travels inside `AskUserQuestion` fields instead of preceding prose. Global anchors land in `plugin/output-styles/oso.md` and `bootstrap/claude-global.md`, and `plugin/skills/_shared/didactic.md` is reconciled to drop its walkthrough references.
- First-run preference round shrinks from three questions to two — explanation depth and adaptive teaching only, the old always/never/offer gating field retired — with a self-healing migration that strips the retired field from any stored `oso/preferences` observation via `mem_update` (merge, never overwrite).

## 0.10.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Windows install path, durable MCP wiring, reachable state, identity-level voice, and a comprehension-gated walkthrough — the harness gets a one-step install for a fresh Windows box, plus the wiring, warmth and comprehension around it. *(Corrected in 0.20.0: "stays wired" was proven by nothing when this was written. Nothing here published the Git Bash the client spawns every `.sh` hook through, `oso-state` resolved to nothing on that box, and the only way to check the result was a shell the same paragraph promised you would not need.)*

- Windows bootstrapper without duplicated logic: double-click `bootstrap\install.bat` → `bootstrap\install.ps1` (PS 5.1-safe; winget-provisions Git, Node.js LTS, and jq per-user; installs Claude Code via the official installer when missing; re-reads the registry PATH for winget lag; honest exit codes) → delegates to the same `install.sh` under Git Bash. `ci.yml` gains a `test-windows` job (hooks tests + `bash -n` + `ps1 -CiMode`).
- MCP wiring made durable: context7 now rides the plugin's own `.mcp.json` (auto-registers on install, no user-scope entry to drift — tool names become `mcp__plugin_oso-code_context7__*`), fallow is provisioned by the installer, and the redundant user-scope context7 is migrated away. The installer never aborts on an MCP failure — it accumulates them and prints an end-of-run per-server summary (OK/FAILED + reason + manual fix); `verify.sh` asserts engram, context7, and fallow present and Connected.
- `oso-state` reachable from every skill: a SessionStart hook exports `OSO_STATE_BIN` to the active plugin's `bin/oso-state` (no-op safe), all skill invocations use `"${OSO_STATE_BIN:-oso-state}"`, and `verify.sh` resolves the active installed plugin version and round-trips `oso-state` through the exact skill form.
- Voice at the identity level: Oso is parcero in every language — Spanish full slang, other languages warmth with occasional markers — under a "short never flattens tone or identity" precedence. The installer sets the `Oso` output style when absent, migrates the old Gentleman style, and respects any other explicit choice.
- Walkthrough contract plus teaching: `/plan` phase 5 delivers a standalone didactic message before approval, then an `AskUserQuestion` comprehension check with a review loop that never reopens decisions, and only then `ExitPlanMode` — walkthrough content in the `ExitPlanMode` argument is banned. A teaching-moment block (three triggers with worked examples) lands in plan/quick/global, and the didactic definition lives once in `plugin/skills/_shared/didactic.md`, referenced by path.

## 0.9.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Walkthrough-before-approval, pana voice, language policy, and index recall — the plan flow tells one story and speaks in one voice:

- Walkthrough moved ahead of approval: the slice plan is presented, the optional end-to-end walkthrough explains it, then a single operator approval is the one gate that starts execution — the former separate "operator says ready" gate is gone. On that approval the plan saves, the `oso/index` row upserts to `executing`, and runtime state initializes.
- Pana voice identity: Oso is a warm Colombian paisa corrector — an expanded seed (parcero, de una, qué pena, pues interspersed) over a technically exacting stance, with the voseo ban and the anti-caricature guard intact.
- English delegations and engram-language policy: the persona styles conversation only — subagent prompts, Agent/Task instructions, and engram technical content are always English and persona-free (stated in the persona scope and the global rules); engram observations carry English content and English titles, narrated in Spanish on request.
- Index recall hardened: `/plan` step 0 self-heals stale `executing` index rows against plan/summary evidence (`mem_update` merge, executing-only guard), and the `oso/index` format is standardized once — rich title `oso/index — {project}: {n} changes, active: {change}`, a `NEXT:` line, the status vocabulary `planning / executing / done / roadmap`, literal topic keys as detail (dash wiki-links banned), roadmap parents listing their children, and explicit non-code pendings.
- The ecommerce project's stale `oso/index` row is corrected at Close (memory-only).

## 0.8.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Operator adaptation — the harness learns how each operator wants to work and meets them there:

- Operator preferences ask once, then apply forever: a single `oso/preferences` engram observation (per-machine `scope: personal`, one upserted row) captures three preferences — E2E walkthrough (always / never / offer each time), explanation depth (concise / standard / didactic), and adaptive teaching (auto-detect / always / off). Asked in `/plan` step 0 on first run only, read silently at every plan/quick start after; `/quick` consumes it but never asks; natural-language changes update it via `mem_update`.
- Optional end-to-end walkthrough before execution: a new `/plan` phase 5 between slice-plan approval and execution — the end-to-end narrative, the slice map, and the risks plus frozen ledger decisions — gated on the walkthrough preference. It explains and never reopens; a decision to revisit goes back through the ledger.
- Gap-evidence adaptive teaching: when a request shows a knowledge gap (contradicts current standard practice, or the operator can't say what the ask involves), the flow briefly explains the terrain and recommends the standard path with the why before executing — wired into `/plan` Intent, `/quick` Micro-intent, and the global rules, with a never-gatekeep guard that stays silent when the operator demonstrates knowledge.
- Oso register fix: Colombian Spanish now addresses the operator with tuteo (tú) and natural usted (e.g. "hágale pues"); voseo is banned (no vos, sabés, tenés, querés, podés).

## 0.7.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Harness audit (5-judge review) — hardening across the rubric, the agents, and the gates:

- Rubric gains operational anchors: soft-trigger thresholds (parameter count, function length, nesting → written justification, never mechanical gates), a countable hard blocker (an abstraction with fewer than two real callers unless a frozen ledger decision names it), file-level single-responsibility, and a stack-translation clause (idioms named in TS/JS, applied in the host language's equivalent).
- Agents rewired: `oso-applier` and `oso-verifier` carry the full rubric; context7 is wired into executable prompts (applier never-guess-a-signature contract, plan/quick decisions verified against current docs); the verifier returns a three-state verdict.
- `/plan` decision rounds close through pre-freeze gates: a battery→ledger reconciliation checklist, an assumption register, and YAGNI citations before the human freezes the ledger.
- `/quick` quality pass now covers the full rubric bar, not a reduced subset.
- Debt-sweep findings are severity-tiered.
- Hooks: wrapper-bypass fix, stale-lock recovery, `log_event` deduplication, and fd-free verify (no `fd` dependency in the verify path).
- Bootstrap: unified CLAUDE.md budget across the install block.
- Cadence precedence: one-question-at-a-time yields to a structured skill's own cadence (e.g. `/plan` decision rounds).

## 0.6.0

- `/plan` gains a Surface mapping phase between Intent and Decision rounds: up to 3 parallel `Explore` subagents build an evidence-based map of what the change touches, and the question battery is generated from that map — every question must cite the code evidence and the consequence of leaving it undecided. The decision-rounds category table is demoted from question generator to blind-spot audit floor, with a fallback to it only when exploration surfaces nothing.
- Engram recall convention: a single `oso/index` observation (one upserted row per change, `status: executing` at Slicing, `status: done` at Close) so resuming a change means searching the index first instead of guessing topic keys. Every ledger, plan, and summary save now carries a rich title (`{topic key} — {human description}`). `/quick` summaries follow the same rich-title rule but are never added to the index.

## 0.5.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

- Model assignments per role: `/plan` orchestrator and the debt-sweep judge pin `opus`; `oso-applier` and `oso-verifier` pin `sonnet`. `/quick` and `quality-pass` keep inheriting the session model. Note: a pinned model is exact, not a minimum — sessions on a higher-tier model will still run pinned components on the pinned tier.

## 0.4.0

Pilot #1 findings (expense-splitter run: 10 slices, 0 false gate blocks, but three quality gaps):

- Debt-sweep is now a pure judge: it reports findings with evidence and never edits; `/plan` must invoke it through the Skill tool (forked subagent — never inline), then an `oso-applier` cleanup assignment fixes the findings, then a fresh sweep confirms. Loop until `clean`.
- Rubric: over-documentation is now an explicit debt marker — zero comments by default, JSDoc only where code cannot be self-evident (with standard description/params/returns shape), why-shaped sentences over trivial code count as WHAT comments, and JSDoc on most exports of a file is itself a violation. (Pilot produced 49 JSDoc blocks across 32 files.)
- Rubric system level: domain logic (validation, normalization, calculation) must live in the domain layer, not UI/component folders.

## 0.3.0

- `/plan` execution is now fully delegated: each slice runs through an `oso-applier` subagent (implements exactly one slice from the frozen ledger; returns a structured `blocked` report instead of ever assuming) and an `oso-verifier` subagent (fresh-context judge with no edit tools — reruns every check itself and returns a verdict with evidence). The orchestrator resolves blocked questions with the human, updates the ledger, and relaunches; it never writes code during execution. `/quick` stays inline. Closes the self-attestation gap flagged by the adversarial review (C9, partially).

## 0.2.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

- `Oso` output style shipped with the plugin: Colombian mentor persona — teaches and corrects with warmth, blocks sloppy work, never agrees without verifying. Persona governs conversation tone only; enforcement stays structural (rubric, hooks, global rules). Activate with `/output-style Oso`.

## 0.1.0

**Reinstall required** — `bootstrap/` changed, and no plugin update carries it: pull the repo and re-run `bash bootstrap/install.sh`.

Initial release.

- `/plan` mode: intent → decision rounds with a frozen ledger → vertical slices → sequential apply/verify at zero warnings → debt-sweep close. Ledger and slice summary go into the PR body on request.
- `/quick` mode: micro-intent → fast visible iteration → quality pass close.
- Shared rubric with judgment contract, hard blockers, and anti-over-extraction rules.
- `debt-sweep` skill (forked context, fallow-assisted on TS/JS, rubric-only elsewhere).
- State-gate hooks (pure bash, fail-safe parsing, tokenized commit matcher, JSONL gate telemetry) backed by the `oso-state` helper (lock-protected atomic writes).
- Cross-OS bootstrap: prerequisites, MCP wiring (engram, context7, fallow), plugin install, marker-based CLAUDE.md merge, and backed-up gentle-ai cleanup behind a confirmation prompt.
