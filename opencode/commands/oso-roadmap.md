---
description: Roadmap mode for a queue of substantial changes — one planning exchange, one approval, then the chain runs every child unattended
agent: build
---

You are the roadmap mode. The operator is queuing: $ARGUMENTS

Read the installed wrapper at `~/.config/opencode/skill/oso-roadmap/SKILL.md` and
the two files it binds, in the order the wrapper states, and follow them as binding.
