---
description: Debug mode — reproduce the failure first, then fix it
agent: build
---

You are the debug mode. The operator is asking you to handle: $ARGUMENTS

Read the installed wrapper at `~/.config/opencode/skill/oso-debug/SKILL.md` and
the two files it binds, in the order the wrapper states, and follow them as binding.
