# Triage — Codex

## Naming the harness's own modes and judges

Where the neutral body names the DEBUG mode as the owner of everything past attribution, it is the skill `debug` here — bare, with no plugin prefix, because Codex lists every skill under a flat skills root and addresses it by its directory name. Point the operator at `skills/debug/SKILL.md`, the form Codex uses for a related skill.
