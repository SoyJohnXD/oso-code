# Milestone reporting — Codex

## No card exists here

This host draws no card for a spawned role or a waited-on handoff: `subagents.md`'s spawn, the wait operation, and `handoff consume` are transport, invisible to the operator unless the milestone text itself says what happened. The contract at `../../reporting.md` is therefore this host's WHOLE visibility layer, not a complement to a native affordance, and it is delivered exactly as every other operator-facing content on this host: ending the turn as plain text, with any tool call in a later turn.
